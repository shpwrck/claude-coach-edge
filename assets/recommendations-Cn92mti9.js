import{H as e}from"./index-C41j_WlH.js";import{a as t,i as n,n as r,o as i,t as a}from"./pricing-C6Aig7EJ.js";import{n as o,t as s}from"./parse-sessions-C-9v-sqA.js";import{C as c,E as l,S as u,T as d,_ as f,b as p,c as m,d as h,f as g,g as _,h as v,l as ee,m as te,n as ne,o as re,p as y,r as ie,s as ae,t as oe,u as se,v as ce,w as b,x as le,y as ue}from"./shared-Qv45YTOL.js";import{t as de}from"./report-card-BIPCQgeQ.js";import{n as x}from"./parse-runtime-events-DbDEuMmW.js";import{t as fe}from"./parse-agent-effectiveness-De_Sc302.js";import{a as pe,l as me,o as he,s as ge,t as _e}from"./parse-tools-D7xOY5zL.js";import{t as ve}from"./parse-tool-effectiveness-DJGlwOxt.js";import{a as ye}from"./cost-attribution-BfwZPZ0H.js";import{t as be}from"./parse-tool-inventory-BvmOwdh_.js";import{a as xe,l as Se,n as Ce,o as we,r as Te,u as Ee}from"./context-health-D6dhrRvO.js";import{n as S,r as De,t as Oe}from"./parse-compaction-risk-BmwD5MOt.js";import{n as ke}from"./parse-file-reread-B-aw0RDB.js";import{a as Ae,i as je}from"./parse-files-CyCZ1IDv.js";import"./parse-tasks-CZ_mG6k_.js";import{t as Me}from"./parse-timeline-success-DmHJw9xu.js";import{i as Ne,o as Pe,r as Fe}from"./parse-permissions-HEQmXOJe.js";import{a as Ie,r as Le}from"./parse-errors-BHBuk0BQ.js";import{r as Re,t as ze}from"./parse-mcp-auth-NkWrU1Lk.js";import{t as Be}from"./parse-stats-cache-Cbxfrjo9.js";var Ve=[`input`,`output`,`cacheWrite5m`,`cacheWrite1h`,`cacheRead`],He={"failed-tool-retry":10,"safety-redo":20,"workflow-rework":30,"activity-idle":35,"structural-prefix":40,"model-tier":50,unclassified:90};function Ue(e){return He[e??`unclassified`]}function C(e,t){return`${e}|${t}`}function w(e,t){return e.tokens[t]/1e6*e.rate[t]}function T(e,n){let r=t(n);switch(e){case`input`:return r.input;case`output`:return r.output;case`cacheWrite5m`:return r.cacheWrite5m;case`cacheWrite1h`:return r.cacheWrite1h;case`cacheRead`:return r.cacheRead}}function We(e){let t=Math.min(e.cacheCreation1hTokens,e.cacheCreationTokens);return{cache1h:t,cache5m:e.cacheCreationTokens-t}}function Ge(e,t){let{cache1h:n,cache5m:r}=We(e);switch(t){case`input`:return e.inputTokens;case`output`:return e.outputTokens;case`cacheWrite5m`:return r;case`cacheWrite1h`:return n;case`cacheRead`:return e.cacheReadTokens}}function Ke(e){return e.webSearchRequests*.01+e.webFetchRequests*0}function qe(e){let t=new Map;for(let n of e)for(let e of n.entries){let r=e.model||`unknown`,i=C(n.sessionId,r),a=t.get(i);a||(a={sessionId:n.sessionId,model:r,tokens:{input:0,output:0,cacheWrite5m:0,cacheWrite1h:0,cacheRead:0},rate:{input:T(`input`,r),output:T(`output`,r),cacheWrite5m:T(`cacheWrite5m`,r),cacheWrite1h:T(`cacheWrite1h`,r),cacheRead:T(`cacheRead`,r)},server:0},t.set(i,a));for(let t of Ve)a.tokens[t]+=Ge(e,t);a.server+=Ke(e)}return t}function Je(e){let t=0;for(let n of e.values()){for(let e of Ve)t+=w(n,e);t+=n.server}return t}var E=1e-9;function D(e,t){let n=new Set,r=[];for(let i of t){if(n.has(i))continue;n.add(i);let t=e.get(i);t&&r.push(t)}return r}function Ye(e,t,n=e=>console.warn(`[reclaim] ${e}`)){let r=qe(t),i=Je(r),a=Xe(e,r,i),o=[...e].sort((e,t)=>Ue(e.cause)-Ue(t.cause)||e.orderKey-t.orderKey||e.leverId.localeCompare(t.leverId)),s=[],c={},l={},u=(e,t)=>(n(`${e.leverId}: ${t}`),{leverId:e.leverId,category:e.category,cause:e.cause??`unclassified`,orderKey:e.orderKey,marginalUsd:0,rejected:!0,rejectReason:t});for(let e of o){let t=e.counterfactual;if(t.kind===`flag-only`){s.push({leverId:e.leverId,category:e.category,cause:e.cause??`unclassified`,orderKey:e.orderKey,marginalUsd:0,rejected:!1});continue}let i=0;if(t.kind===`directUsd`){let n=0,a=D(r,e.scopeKeys);if(a.length===0){s.push(u(e,`directUsd claim resolves to no priced scope`));continue}for(let e of a)n+=e.server;if(t.usd>n+E){s.push(u(e,`directUsd ${t.usd.toFixed(4)} exceeds server-fee residual ${n.toFixed(4)}`));continue}let o=t.usd;for(let e of a){let t=Math.min(e.server,o);if(e.server-=t,o-=t,i+=t,o<=E)break}}else{let a=D(r,e.scopeKeys);if(a.length===0){s.push(u(e,`claim resolves to no priced scope (precondition)`));continue}for(let r of a)for(let a of e.ownedPools){let o=w(r,a);if(o<=E)continue;let s=Ze(r,a,t),c=o-s.cost;if(!(c<=E)){if(c>o+E){n(`${e.leverId}: ${a}@${C(r.sessionId,r.model)} would over-draw residual (drop ${c.toFixed(6)} > ${o.toFixed(6)})`);continue}r.tokens[a]=s.tokens,r.rate[a]=s.rate,i+=c}}}s.push({leverId:e.leverId,category:e.category,cause:e.cause??`unclassified`,orderKey:e.orderKey,marginalUsd:i,rejected:!1}),c[e.category]=(c[e.category]??0)+i,l[e.leverId]=(l[e.leverId]??0)+i}let d=Je(r),f=i-d,p=s.reduce((e,t)=>e+t.marginalUsd,0);return Math.abs(p-f)>1e-6&&n(`cascade identity broken: sum(marginal)=${p.toFixed(6)} vs billOriginal-billFinal=${f.toFixed(6)}`),{billOriginal:i,billFinal:d,total:f,byCategory:c,byLever:l,coverageByCategory:a,booked:s}}function Xe(e,t,n){let r=new Map,i=e=>{let t=r.get(e);return t||(t=new Set,r.set(e,t)),t};for(let n of e){let e=i(n.category),r=n.counterfactual,a=D(t,n.scopeKeys);if(r.kind===`directUsd`){for(let t of a)t.server>E&&e.add(`${C(t.sessionId,t.model)}#server`);continue}for(let t of a){let r=C(t.sessionId,t.model);for(let i of n.ownedPools)w(t,i)>E&&e.add(`${r}#${i}`)}}let a={};for(let[e,i]of r){let r=0;for(let e of i){let n=e.lastIndexOf(`#`),i=e.slice(0,n),a=e.slice(n+1),o=t.get(i);o&&(r+=a===`server`?o.server:w(o,a))}r=Math.min(r,n),!(r<=E)&&(a[e]={claimedUsd:r,totalBill:n,coverage:n>E?r/n:0})}return a}function Ze(e,t,n){let r=e.tokens[t],i=e.rate[t];switch(n.kind){case`scaleTokens`:{let e=n.poolDeltaFrac[t]??0,a=r*Math.max(0,1-e);return{tokens:a,rate:i,cost:a/1e6*i}}case`reprice`:{let e=Math.min(i,T(t,n.toModel));return{tokens:r,rate:e,cost:r/1e6*e}}case`convertRate`:{if(t!==n.rateFrom)return{tokens:r,rate:i,cost:w(e,t)};let a=Math.min(i,T(n.rateTo,e.model));return{tokens:r,rate:a,cost:r/1e6*a}}}}function Qe(e){return{total:e.total,byCategory:e.byCategory,byLever:e.byLever,coverageByCategory:e.coverageByCategory,totalBill:e.billOriginal}}var $e={headings:[/^##\s+Cach(e|ing)\b/i],bodyPhrases:[`default 5-minute prompt cache for routine work`]},et={id:`cost.cache-1h-waste`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(y(e.liveConfig,$e))return null;let n=0,r=0,i=new Set,a=new Set;for(let o of e.tokenData)for(let e of o.entries){let s=Math.min(e.cacheCreation1hTokens,e.cacheCreationTokens);if(s<=0)continue;let c=t(e.model);n+=s/1e6*(c.cacheWrite1h-c.cacheWrite5m),r+=s,i.add(o.sessionId),a.add(C(o.sessionId,e.model||`unknown`))}if(n<.05)return null;let o={leverId:`cost.cache-1h-waste`,category:`cost`,orderKey:50,ownedPools:[`cacheWrite1h`],scopeKeys:[...a],counterfactual:{kind:`convertRate`,rateFrom:`cacheWrite1h`,rateTo:`cacheWrite5m`},evidenceTokens:r};return{id:`cost.cache-1h-waste`,category:`cost`,severity:n>=1?`warning`:`info`,title:`Reduce 1-hour cache writes`,detail:`${(r/1e6).toFixed(2)}M tokens were written to the 1-hour cache across ${i.size} session(s), billed at 2× input vs 1.25× for the default 5-minute cache.`,action:`If that context is not reused beyond 5 minutes, prefer the 5-minute cache — up to the amount shown is recoverable.`,estSavingsUsd:n,reclaim:o,affected:i.size,view:`tokens`,fix:{target:`CLAUDE.md`,label:`Note the 1h-cache tradeoff`,note:`Paste into CLAUDE.md so sessions avoid the 2× cache write when context is short-lived. No settings key controls cache TTL.`,snippet:`## Caching
- Prefer the default 5-minute prompt cache for routine work; it is billed at 1.25× input vs 2× for the 1-hour cache.
- Only rely on the 1-hour cache for context genuinely reused across gaps >5 min. For short, scoped tasks, finish or compact before idle gaps so context is not re-written to the pricier 1h cache.`,appliedMarkers:$e}}}},tt={headings:[/^##\s+Session scope\b/i],bodyPhrases:[`start a fresh session when the task changes`]},nt={id:`cost.expensive-sessions`,category:`cost`,dataDeps:[`tokenData`,`toolData`,`liveConfig`],rule(e){if(y(e.liveConfig,tt))return null;let t=ye(e.tokenData,e.toolData,5),n=e.tokenData.reduce((e,t)=>e+s(t),0),r=t.slice(0,3),i=r.reduce((e,t)=>e+t.estimatedCost,0);if(n<=0||i<.05)return null;let a=i/n*100;return a<25||e.tokenData.length<5?null:{id:`cost.expensive-sessions`,category:`cost`,severity:`info`,title:`Spend is concentrated in a few sessions`,detail:`The top 3 sessions account for ${a.toFixed(0)}% of total estimated spend (${v(i)} of ${v(n)}).`,action:`Review these sessions for runaway context or repeated work that could be scoped down or split.`,estSavingsUsd:i,affected:r.length,evidence:r.map(e=>`${d(e.sessionId)}, ${v(e.estimatedCost)}, ${e.topTool??`no tools`}`),view:`cost`,fix:{target:`CLAUDE.md`,label:`Add session-scoping guidance`,note:`Paste into CLAUDE.md to curb the runaway-context pattern behind the top sessions.`,snippet:`## Session scope
- Keep one session to one task; start a fresh session when the task changes so stale context is not re-sent and re-billed each turn.
- Compact or clear once a sub-task is done rather than letting context grow unbounded.
- Avoid re-reading large files or directories repeatedly in a single session; reference them once.`,appliedMarkers:tt}}}};function rt(e){let t=e.targetModel??a,n=e.minBaselineEntries??1,r=e.minComparisonEntries??1,i=e.minComparisonTargetShare??.5,o=at(e.tokenData,t);if(o.length<n+r)return null;let s=null;for(let e=n;e<=o.length-r;e+=1){if(!o[e].targetPriced)continue;let t=o.slice(0,e),n=o.slice(e);if(n.filter(e=>e.targetPriced).length/n.length<i)continue;let r=ot(t)-ot(n);r<=0||(!s||r>s.realizedSavingsUsd)&&(s={index:e,realizedSavingsUsd:r})}if(!s)return null;let c=o.slice(0,s.index),l=o.slice(s.index),u={baseline:{start:new Date(c[0].timestampMs).toISOString(),end:new Date(l[0].timestampMs).toISOString()},comparison:{start:new Date(l[0].timestampMs).toISOString(),end:new Date(l[l.length-1].timestampMs+1).toISOString()},targetModel:t};return it({tokenData:e.tokenData,...u})?.realizedSavingsUsd?u:null}function it(e){let t=e.targetModel??a,n=st(e.tokenData,e.baseline,t),r=st(e.tokenData,e.comparison,t);if(n.length===0||r.length===0)return null;let i=lt(n),o=lt(r),s=Math.max(0,i.premiumUsd-o.premiumUsd),c=o.premiumUsd;return{targetModel:t,baseline:i,comparison:o,realizedSavingsUsd:s,predictedSavingsUsd:c,attribution:{interventionKey:e.interventionKey??`cost.automation-share`,signatureId:e.signatureId??`automation-model-pin`,tier:`tier-1-before-after`,predictedSavingsUsd:c,realizedSavingsUsd:s,confidence:`medium`,window:{baseline:e.baseline,comparison:e.comparison}}}}function at(e,t){let r=[];for(let a of e)if(o(a.entrypoint))for(let e of a.entries){let o=new Date(e.timestamp).getTime();if(!Number.isFinite(o))continue;let s=e.model||a.model||`unknown`;if(i(s).isSynthetic)continue;let c=n(e,s),l=n(e,t);r.push({sessionId:a.sessionId,model:s,actualCost:c,targetCost:l,timestampMs:o,targetPriced:c<=l+1e-6})}return r.sort((e,t)=>e.timestampMs-t.timestampMs)}function ot(e){return e.reduce((e,t)=>e+Math.max(0,t.actualCost-t.targetCost),0)}function st(e,t,r){let a=[];for(let s of e)if(o(s.entrypoint))for(let e of s.entries){if(!ct(e.timestamp,t))continue;let o=e.model||s.model||`unknown`;if(i(o).isSynthetic)continue;let c=n(e,o),l=n(e,r);a.push({sessionId:s.sessionId,model:o,actualCost:c,targetCost:l})}return a}function ct(e,t){let n=new Date(e).getTime(),r=new Date(t.start).getTime(),i=new Date(t.end).getTime();return!Number.isFinite(n)||!Number.isFinite(r)||!Number.isFinite(i)?!1:n>=r&&n<i}function lt(e){let t=new Set,n=new Map,r=0,i=0;for(let a of e){t.add(a.sessionId),r+=a.actualCost,i+=a.targetCost;let e=n.get(a.model)??[];e.push(a),n.set(a.model,e)}return{entries:e.length,sessions:t.size,actualModelSpendUsd:r,targetModelSpendUsd:i,premiumUsd:Math.max(0,r-i),modelMix:Array.from(n.entries()).map(([e,t])=>ut(e,t)).sort((e,t)=>{let n=t.premiumUsd-e.premiumUsd;return n===0?e.model<t.model?-1:+(e.model>t.model):n})}}function ut(e,t){let n=new Set(t.map(e=>e.sessionId)),r=t.reduce((e,t)=>e+t.actualCost,0),i=t.reduce((e,t)=>e+t.targetCost,0);return{model:e,entries:t.length,sessions:n.size,actualModelSpendUsd:r,targetModelSpendUsd:i,premiumUsd:Math.max(0,r-i)}}var dt=[`input`,`output`,`cacheWrite5m`,`cacheWrite1h`,`cacheRead`],ft={id:`cost.automation-share`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`,`modelPinSavings`],rule(e){let t=ue(e.liveConfig?.settings),{autoCost:r,total:s,share:c}=se(e.tokenData),l=0,u=0,d=new Set,f=new Set;for(let t of e.tokenData)if(o(t.entrypoint)){d.add(t.sessionId);for(let e of t.entries){let r=e.model||`unknown`;if(i(r).isSynthetic)continue;let o=n(e,r)-n(e,a);o>0&&(l+=o,f.add(C(t.sessionId,r)),u+=e.inputTokens+e.outputTokens+e.cacheCreationTokens+e.cacheReadTokens)}}let p={leverId:`cost.automation-share`,category:`cost`,orderKey:80,ownedPools:dt,scopeKeys:[...f],counterfactual:{kind:`reprice`,toModel:a},evidenceTokens:u},m=e.modelPinSavings?it({tokenData:e.tokenData,baseline:e.modelPinSavings.baseline,comparison:e.modelPinSavings.comparison,targetModel:e.modelPinSavings.targetModel}):null;if(!m?.attribution&&(t||r<1||s<=0||c<15))return null;let h=l>=.01?` Running those automation turns on Haiku instead would have cost about ${v(l)} less.`:``;return{id:`cost.automation-share`,category:`cost`,severity:`info`,title:`Automation drives a large share of spend`,detail:`Automated (sdk-*) sessions account for ${v(r)} (${c.toFixed(0)}% of total) across ${d.size} session(s).${h}`,action:t?`Keep automated runs on the cheapest model that meets the quality bar; the observed before/after savings are shown on this card.`:`Confirm automated runs use the cheapest model that meets the quality bar — Haiku/Sonnet often suffice for scripted work.`,estSavingsUsd:l,reclaim:p,...m?.attribution?{savingsAttribution:m.attribution}:{},affected:d.size,view:`cost`,...t?{}:{fix:{target:`settings.json`,label:`Default automation to Haiku`,note:`Merge into the settings.json your sdk-* runs use, to right-size the ${c.toFixed(0)}% automated spend. Use the cheapest model that holds your quality bar.`,snippet:`{
  "model": "claude-haiku-4-5"
}`}}}}},pt={id:`cost.unknown-model`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`],rule(t){if(p(t.liveConfig?.settings))return null;let n=t.tokenData.filter(e=>e.hasUnknownModel);return n.length===0?null:{id:`cost.unknown-model`,category:`cost`,severity:`info`,title:`Some cost estimates are guesses`,detail:`${n.length} session(s) used a model string not in the pricing table and fell back to Sonnet-tier rates.`,action:`Treat those sessions’ cost figures as approximate; add the model to the pricing table for accuracy.`,reclaim:{leverId:`cost.unknown-model`,category:`cost`,orderKey:85,ownedPools:[],scopeKeys:[],counterfactual:{kind:`flag-only`},evidenceTokens:0},affected:n.length,view:`tokens`,fix:{target:`settings.json`,label:`Pin a known model`,note:`Set an explicit model in settings.json so future sessions are priced from a known rate instead of the Sonnet-tier fallback used for the ${n.length} guessed session(s).`,snippet:`{\n  "model": "${e.sonnet}"\n}`}}}},O=e.opus,k=t(`claude-opus-4-1-20250414`),mt=t(O),ht=k.input-mt.input,gt=.5,_t={id:`cost.legacy-model-overpay`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`],rule(e){let n=e.liveConfig?.settings?.model;if(p(e.liveConfig?.settings)&&typeof n==`string`&&t(n)!==k)return null;let r=0,i=0,a=new Set,o=new Set;for(let n of e.tokenData)for(let e of n.entries){if(t(e.model)!==k)continue;let s=e.inputTokens/1e6*ht;s>0&&(r+=s,i+=e.inputTokens,a.add(n.sessionId),o.add(C(n.sessionId,e.model||`unknown`)))}if(r<gt)return null;let s={leverId:`cost.legacy-model-overpay`,category:`cost`,orderKey:55,ownedPools:[`input`],scopeKeys:[...o],counterfactual:{kind:`reprice`,toModel:O},evidenceTokens:i};return{id:`cost.legacy-model-overpay`,category:`cost`,severity:r>=1?`warning`:`info`,title:`Switch off legacy-priced Opus to current Opus`,detail:`${v(r)} of spend ran on claude-opus-4-1-* (legacy $15/MTok input) across ${a.size} session(s); the same capability tier on current Opus is $5/MTok — a ~67% overpay on those turns.`,action:`Update the pinned model string (settings.json or the SDK caller) from claude-opus-4-1-* to ${O} or newer.`,estSavingsUsd:r,reclaim:s,affected:a.size,evidence:[...a].slice(0,5).map(e=>d(e)),view:`cost`,fix:{target:`settings.json`,label:`Pin current Opus`,note:`Set the model to a current-tier Opus in settings.json (or the SDK config that launches these runs). Suppressed once a non-legacy model is pinned.`,snippet:`{\n  "model": "${O}"\n}`}}}},vt=.5,yt=.1,bt={headings:[/^##\s+Web-search discipline\b/i],bodyPhrases:[`Prefer web_fetch for stable URLs`]},xt={id:`cost.web-search-spend`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(y(e.liveConfig,bt))return null;let t=0,n=0,i=new Set,a=new Set;for(let r of e.tokenData){n+=s(r);for(let e of r.entries)e.webSearchRequests>0&&(t+=e.webSearchRequests,i.add(r.sessionId),a.add(C(r.sessionId,e.model||`unknown`)))}let o=t*r.webSearchRequest;if(o<vt||n<=0||o/n<=yt)return null;let c=Math.round(o/n*100),l={leverId:`cost.web-search-spend`,category:`cost`,orderKey:60,ownedPools:[],scopeKeys:[...a],counterfactual:{kind:`directUsd`,usd:o},evidenceTokens:0};return{id:`cost.web-search-spend`,category:`cost`,severity:`info`,title:`Web-search requests are a material cost driver`,detail:`Web searches cost ~${v(o)} total ($0.01/request, ${t} requests across ${i.size} session(s)) — about ${c}% of estimated spend. Research loops often re-fetch the same queries each turn.`,action:`Prefer web_fetch for known stable URLs (no per-request charge), batch related lookups into one broad query, and cache fetched content in a file rather than re-searching.`,estSavingsUsd:o,reclaim:l,affected:i.size,evidence:[...i].slice(0,5).map(e=>d(e)),view:`cost`,fix:{target:`CLAUDE.md`,label:`Add web-search discipline`,note:`Append to CLAUDE.md so research loops stop re-charging for repeated searches.`,snippet:`## Web-search discipline

- Prefer web_fetch for stable URLs (no per-request charge); web_search bills $0.01/request.
- Batch related lookups into one broad query instead of many narrow ones.
- Cache fetched content in a file — do not re-fetch the same URL across turns.`,appliedMarkers:bt}}}},St=3,Ct={id:`cost.priority-tier-spend`,category:`cost`,dataDeps:[`tokenData`],rule(e){let t=e.tokenData.filter(e=>e.serviceTier===`priority`);return t.length<St?null:{id:`cost.priority-tier-spend`,category:`cost`,severity:`info`,title:`Sessions ran on the priority service tier — confirm intent`,detail:`${t.length} session(s) used service_tier='priority' (a rate premium over standard); for routine automation the standard tier usually suffices.`,action:`Review which sessions/automation used the priority tier; ensure no SDK client or env var forces priority for non-latency-sensitive work.`,reclaim:{leverId:`cost.priority-tier-spend`,category:`cost`,orderKey:85,ownedPools:[],scopeKeys:[],counterfactual:{kind:`flag-only`},evidenceTokens:0},affected:t.length,evidence:t.slice(0,5).map(e=>d(e.sessionId)),view:`cost`}}},A=3;function wt(e){let t=/^mcp__([^_]+(?:_[^_]+)*?)__/.exec(e);return t?t[1]:null}var Tt={id:`cost.idle-mcp-tools`,category:`cost`,dataDeps:[`toolInventories`,`liveConfig`],rule(e){let t=e.toolInventories;if(!t||t.length===0)return null;let n=be(t).unusedByFrequency.filter(e=>e.toolName.startsWith(`mcp__`)&&e.usedIn===0&&e.neverUsed>=A);if(n.length===0)return null;let r=e.liveConfig!=null,i=new Set((e.liveConfig?.mcpServers??[]).map(e=>e.id));if(r&&(n=n.filter(e=>{let t=wt(e.toolName);return t!=null&&i.has(t)}),n.length===0))return null;let a=Array.from(new Set(n.map(e=>wt(e.toolName)).filter(e=>!!e)));return{id:`cost.idle-mcp-tools`,category:`cost`,severity:`info`,title:`Remove MCP tools loaded but never invoked`,detail:`${n.length} MCP tool(s) were loaded in ${A}+ sessions yet invoked zero times; each adds ~50-200 tokens to the tool manifest on every turn of every session that loads it.`,action:`Disable or remove the MCP server(s) backing the never-used tool(s) from ~/.claude.json (or scope them to the projects that need them).`,reclaim:{leverId:`cost.idle-mcp-tools`,category:`cost`,orderKey:85,ownedPools:[],scopeKeys:[],counterfactual:{kind:`flag-only`},evidenceTokens:0},affected:n.length,evidence:n.slice(0,5).map(e=>`${e.toolName} — loaded in ${e.loadedIn}, used 0`),provenance:{observations:[{claim:`${n.length} MCP tool(s) loaded in ${A}+ sessions, invoked 0 times`,source:`parse-tool-inventory`,field:`aggregateInventory().unusedByFrequency (usedIn === 0)`,value:n.length},{claim:r?`backing server(s) still configured: ${a.join(`, `)||`(none)`}`:`current MCP config could not be read; server list not reconciled`,source:`~/.claude.json`,field:`mcpServers[].id`}],inference:`Each idle tool adds ~50-200 tokens to the tool manifest on every turn of every session that loads it; removing the still-configured backing server(s) recovers that overhead.`},view:`tools`,fix:{target:`command`,label:`Disable idle MCP servers`,note:`Remove or scope these mcpServers in ~/.claude.json (or move them into a per-project .mcp.json). Servers with no invoked tools: ${a.join(`, `)||`(see evidence)`}.`,snippet:a.length?`# In ~/.claude.json, delete the "mcpServers" entries for:\n${a.map(e=>`#   - ${e}`).join(`
`)}`:`# In ~/.claude.json, delete the unused "mcpServers" entries (see the tool names above).`}}}},Et=.1,Dt=5,Ot={id:`cost.expensive-agent-type`,category:`cost`,dataDeps:[`agentSettings`,`attribution`,`runtimeEvents`,`toolData`,`tokenData`],rule(e){if(ue(e.liveConfig?.settings))return null;let t=fe(e.agentSettings??[],e.attribution??[],e.runtimeEvents??[],e.toolData,e.tokenData).filter(e=>e.meanCostUsd>Et&&e.runs>=Dt);if(t.length===0)return null;t.sort((e,t)=>t.meanCostUsd-e.meanCostUsd);let n=t[0];return{id:`cost.expensive-agent-type`,category:`cost`,severity:`info`,title:`High mean cost per agent run`,detail:`Agent type "${n.agentType}" averages ${v(n.meanCostUsd)}/run over ${n.runs} runs${t.length>1?` (and ${t.length-1} other type(s) over $0.10/run)`:``}; right-sizing its model recovers most of that.`,action:`Pin a cheaper model (e.g. claude-haiku-4-5) for that agent type, narrow its task scope, or cap the context passed to it.`,reclaim:{leverId:`cost.expensive-agent-type`,category:`cost`,orderKey:85,ownedPools:[],scopeKeys:[],counterfactual:{kind:`flag-only`},evidenceTokens:0},affected:t.length,evidence:t.slice(0,5).map(e=>`${e.agentType}: ${v(e.meanCostUsd)}/run × ${e.runs}`),view:`agents`,fix:{target:`settings.json`,label:`Pin a cheaper model for the agent`,note:`Set a cheaper model in the SDK config / settings.json that launches the flagged agent type.`,snippet:`{
  "model": "claude-haiku-4-5"
}`}}}},kt={headings:[/^##\s+Context discipline\b/i],bodyPhrases:[`working context well under the model`]},At={id:`context.over-window`,category:`context`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(y(e.liveConfig,kt))return null;let t=we(e.tokenData).filter(e=>e.peakContext>Te);return t.length===0?null:{id:`context.over-window`,category:`context`,severity:`warning`,title:`Sessions spilled past the 200K context window`,detail:`${t.length} session(s) peaked above 200K tokens, where context is evicted and re-sent at cost.`,action:`Compact earlier or start a fresh session once a task is done — keep working context well under the window.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${d(e.sessionId)}, peak ${(e.peakContext/1e3).toFixed(0)}k`),view:`context`,fix:{target:`CLAUDE.md`,label:`Compact earlier`,note:`Append to your project (or ~/.claude) CLAUDE.md so Claude trims context before the window fills.`,snippet:`## Context discipline

Keep the working context well under the model's context window.
- When the conversation grows large (roughly 150K+ tokens) or right after finishing a discrete task, run \`/compact\` to summarise and reclaim space.
- When starting genuinely unrelated work, run \`/clear\` to begin a fresh session instead of carrying stale context.
- Avoid pulling large files or command output into context the current task doesn't need.`,appliedMarkers:kt}}}},jt={headings:[/^##\s+(Keep the session|Session) heal/i],bodyPhrases:[`Avoid switching permission modes mid-session`]},Mt={id:`context.low-health`,category:`context`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(y(e.liveConfig,jt))return null;let t=Ee(e.tokenData).filter(e=>e.score<50);return t.length===0?null:{id:`context.low-health`,category:`context`,severity:`warning`,title:`Low session-health scores`,detail:`${t.length} session(s) scored below 50 on the context-health heuristic (cache reuse, compaction, growth).`,action:`Open Context Health to see the per-session reasons and address the worst offenders.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${d(e.sessionId)}, ${e.score}, ${e.reasons[0]??``}`),view:`context`,fix:{target:`CLAUDE.md`,label:`Keep context healthy`,note:`Append to CLAUDE.md to curb the growth/churn that drags the health score down.`,snippet:`## Keep the session healthy

- Read a file once and work from that context; don't re-Read unchanged files.
- Don't load large or unrelated files "just in case" — pull only what the current task needs.
- After completing a task, \`/compact\` (to keep going) or \`/clear\` (for unrelated next work) rather than letting context grow unbounded.
- Avoid switching permission modes mid-session; it churns context and hurts cache reuse.`,appliedMarkers:jt}}}},Nt={headings:[/^##\s+(Keep the )?prompt cache\b/i],bodyPhrases:[`stable context prefix, so avoid churning`]},Pt={id:`context.low-cache-hit`,category:`context`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(y(e.liveConfig,Nt))return null;let t=xe(e.tokenData).filter(e=>e.totalReads+e.totalWrites>0);if(t.length===0)return null;let n=t.filter(e=>e.hitRate<Ce);if(n.length===0)return null;let r=t.reduce((e,t)=>e+t.hitRate,0)/t.length,i=new Set(n.map(e=>e.sessionId)),a=new Map(n.map(e=>[e.sessionId,Se(e.hitRate)])),o=new Set,s=0,c=0;for(let t of e.tokenData){if(!i.has(t.sessionId))continue;let e=a.get(t.sessionId)??0;for(let n of t.entries){let r=n.cacheCreationTokens;r<=0||(o.add(C(t.sessionId,n.model||`unknown`)),s+=r*e,c+=r)}}let l=c>0?s/c:0,u=c>0&&l>0?{leverId:`context.low-cache-hit`,category:`context`,cause:`structural-prefix`,orderKey:60,ownedPools:[`cacheWrite5m`,`cacheWrite1h`],scopeKeys:[...o],counterfactual:{kind:`scaleTokens`,poolDeltaFrac:{cacheWrite5m:l,cacheWrite1h:l}},evidenceTokens:c}:void 0;return{id:`context.low-cache-hit`,category:`context`,severity:`info`,title:`Low cache hit rate on some sessions`,detail:`${n.length} session(s) read back less than ${(Ce*100).toFixed(0)}% of cached context (overall average ${(r*100).toFixed(0)}%). Low reuse means more tokens billed at full input rate.`,action:`Avoid frequent context churn within a session (large unrelated reads, mode switches) so the prompt cache stays warm.`,...u?{reclaim:u}:{},affected:n.length,view:`context`,fix:{target:`CLAUDE.md`,label:`Keep the cache warm`,note:`Append to CLAUDE.md so context stays stable within a session and the prompt cache is reused.`,snippet:`## Keep the prompt cache warm

Cache hits require a stable context prefix, so avoid churning it mid-session:
- Don't interleave large, unrelated file reads into focused work — batch related reads together.
- Avoid switching permission modes or models in the middle of a task.
- Don't re-Read files that haven't changed; reuse what's already in context.
- Group similar work so repeated context (same files, same instructions) is reused rather than re-sent at full input rate.`,appliedMarkers:Nt}}}},Ft=200,It=400,Lt={id:`context.bloated-claude-md`,category:`context`,dataDeps:[`liveConfig`],rule(e){let t=le(e.liveConfig);if(t.length===0)return null;let n=t.split(`
`).length;return n<=Ft?null:{id:`context.bloated-claude-md`,category:`context`,severity:n>It?`critical`:`warning`,title:`Trim the oversized global CLAUDE.md`,detail:`Your merged global CLAUDE.md is ~${n} lines, over Anthropic's documented ${Ft}-line target; every session pays that token cost and instruction adherence degrades past it.`,action:"Move reference material into path-scoped .claude/rules/*.md (with a `paths:` frontmatter) or skills with disable-model-invocation, so rules load only when relevant files are touched.",affected:n,fix:{target:`CLAUDE.md`,label:`Split rules out of CLAUDE.md`,note:`Keep CLAUDE.md near the ~200-line target. Move topic-specific reference material into path-scoped rule files that load only when matching files are touched.`,snippet:`## Instructions size — split rules

Keep this file near Anthropic's ~200-line target. Past it, token cost rises every
session and instruction adherence degrades.

- Move topic-specific guidance into \`.claude/rules/<topic>.md\` with a \`paths:\`
  frontmatter so each rule loads only when a matching file is touched:

  \`\`\`markdown
  ---
  paths: ["src/payments/**"]
  ---
  # Payments conventions
  …
  \`\`\`

- Or move rarely-needed reference material into a skill with
  \`disable-model-invocation\` so it is read on demand, not every turn.`}}}},Rt=2,zt=40,Bt={headings:[/^##\s+Context discipline\b/i],bodyPhrases:[`letting context grow until it auto-compacts`]},Vt={id:`context.compaction-hot-sessions`,category:`context`,dataDeps:[`tokenData`,`toolData`,`timelines`,`liveConfig`],rule(e){if(y(e.liveConfig,Bt))return null;let t=S(e.tokenData,e.toolData,e.timelines??[]),n=De(t);if(n.hotSessions<Rt&&n.hotPercent<zt)return null;let r=n.topSuggestion?` Dominant fix for the cohort: ${n.topSuggestion}.`:``,i=new Set(t.filter(e=>e.riskClass!==`low`).map(e=>e.sessionId)),a=new Map(xe(e.tokenData).filter(e=>i.has(e.sessionId)).map(e=>[e.sessionId,Se(e.hitRate)])),o=new Set,s=0,c=0;for(let t of e.tokenData){if(!i.has(t.sessionId))continue;let e=a.get(t.sessionId)??0;for(let n of t.entries){let r=n.cacheCreationTokens;r<=0||(o.add(C(t.sessionId,n.model||`unknown`)),s+=r*e,c+=r)}}let l=c>0?s/c:0,u=c>0&&l>0?{leverId:`context.compaction-hot-sessions`,category:`context`,cause:`structural-prefix`,orderKey:62,ownedPools:[`cacheWrite5m`,`cacheWrite1h`],scopeKeys:[...o],counterfactual:{kind:`scaleTokens`,poolDeltaFrac:{cacheWrite5m:l,cacheWrite1h:l}},evidenceTokens:c}:void 0;return{id:`context.compaction-hot-sessions`,category:`context`,severity:`warning`,title:`Multiple sessions at high compaction risk`,detail:`${n.hotSessions} session(s) (${n.hotPercent.toFixed(0)}% of the fleet) are in the high compaction-risk band; uncontrolled compaction re-sends 10–40K tokens per event.`,action:`Run /compact at task boundaries, /clear when switching tasks, and scope Read with offset/limit.${r}`,...u?{reclaim:u}:{},affected:n.hotSessions,view:`context`,fix:{target:`CLAUDE.md`,label:`Add context discipline`,note:`Append to CLAUDE.md so sessions trim context before they thrash into repeated compaction.`,snippet:"## Context discipline\n\n- Run `/compact` at sub-task boundaries and `/clear` when switching tasks, rather than letting context grow until it auto-compacts.\n- Read with `offset`/`limit` and prefer Grep over reading whole large files.\n- Don't pull large or unrelated files into context the current task doesn't need.",appliedMarkers:Bt}}}},Ht=2,Ut={headings:[/^##\s+Session resets\b/i],bodyPhrases:[`split the remaining work into a new session`]},Wt={id:`context.repeated-compactions`,category:`context`,dataDeps:[`tokenData`,`toolData`,`timelines`,`liveConfig`],rule(e){if(y(e.liveConfig,Ut))return null;let t=S(e.tokenData,e.toolData,e.timelines??[]).filter(e=>e.compactionsObserved>=2);return t.length<Ht?null:{id:`context.repeated-compactions`,category:`context`,severity:`info`,title:`Sessions compacting multiple times — chronic context pressure`,detail:`${t.length} session(s) compacted 2+ times; each carried compacted summary re-bills tokens on every subsequent turn.`,action:`After a compaction, /clear and start fresh when switching tasks rather than carrying the summary forward; split remaining work into a new session.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${d(e.sessionId)}, ${e.compactionsObserved} compactions`),view:`context`,fix:{target:`CLAUDE.md`,label:`Add session-reset guidance`,note:`Append to CLAUDE.md so a second compaction triggers a clean reset instead of carrying the summary forward.`,snippet:`## Session resets

- If a session compacts twice, run \`/clear\` and split the remaining work into a new session rather than carrying the compacted summary forward — it re-bills on every subsequent turn.`,appliedMarkers:Ut}}}},Gt=3,Kt={headings:[/^##\s+Tool output discipline\b/i],bodyPhrases:[`prefer Grep/Glob over unfiltered`]},qt={id:`context.compaction-large-tool-outputs`,category:`context`,dataDeps:[`tokenData`,`toolData`,`timelines`,`liveConfig`],rule(e){if(y(e.liveConfig,Kt))return null;let t=S(e.tokenData,e.toolData,e.timelines??[]),n=t.filter(e=>e.riskClass!==`low`);if(n.length===0)return null;let r=n.reduce((e,t)=>e+t.largeToolOutputRate,0)/n.length,i=t.filter(e=>e.topFactor===`tool-output`).length;return r<.1||i<Gt?null:{id:`context.compaction-large-tool-outputs`,category:`context`,severity:`info`,title:`Large tool outputs are inflating context`,detail:`Across hot sessions the mean large-tool-output rate is ${Math.round(r*100)}% (over the ${Math.round(Oe*100)}% warn threshold), with tool output the dominant compaction factor on ${i} session(s).`,action:`Scope Read with offset/limit and prefer Grep over unfiltered cat so tool outputs stay under the large-output threshold.`,affected:i,view:`context`,fix:{target:`CLAUDE.md`,label:`Add tool-output discipline`,note:`Append to CLAUDE.md so large tool outputs stop dominating context growth.`,snippet:"## Tool output discipline\n\n- Read with `offset`/`limit` instead of whole large files; prefer Grep/Glob over unfiltered `cat` so a single tool result stays small.\n- Don't echo large command output into context the current task doesn't need.",appliedMarkers:Kt}}}},Jt={headings:[/^##\s+Stable reference files\b/i],bodyPhrases:[`Reference these stable files instead of re-reading them`]};function Yt(e){return!e.churn||e.churn.churn===0}function Xt(e){return e.symbols.some(e=>e.exported)}function j(e){return h(e).replace(/\.[cm]?[jt]sx?$/,``)}function Zt(e){let t=new Map;for(let n of e.files){let e=new Set;for(let r of n.imports){let n=j(r);!n||e.has(n)||(e.add(n),t.set(n,(t.get(n)??0)+1))}}return t}function Qt(e,t){let n=j(e.path);return n?(t.get(n)??0)-+!!e.imports.some(e=>j(e)===n):0}var $t=2,en=5,tn=3,nn={id:`context.repo-map-context-waste`,category:`context`,dataDeps:[`repoMap`,`toolData`,`tokenData`,`liveConfig`],rule(e){if(y(e.liveConfig,Jt))return null;let t=e.repoMap;if(!t||t.projects.length===0)return null;let n=[];for(let e of t.projects){let t=Zt(e);for(let r of e.files){if(!r.reread||r.reread.totalEstimatedTokenWaste<=0||!Yt(r))continue;let e=Xt(r),i=r.configSections.length>0,a=Qt(r,t);if(!e&&!i&&!(a>=$t))continue;let o=i?`config-backed`:e?`stable-api`:`high-centrality`;n.push({path:r.path,reason:o,rereadTokens:r.reread.totalEstimatedTokenWaste,sessions:r.reread.sessions,symbols:r.symbols.filter(e=>e.exported).slice(0,tn).map(e=>e.name),configSections:r.configSections,centrality:a})}}if(n.length===0)return null;n.sort((e,t)=>t.rereadTokens-e.rereadTokens||t.centrality-e.centrality);let r=n.slice(0,en),i=n.reduce((e,t)=>e+t.rereadTokens,0),a=new Set(r.map(e=>e.path)),o=ke(e.toolData,e.tokenData),s=new Map;for(let e of o.repeats)e.estimatedTokenWaste<=0||[...a].some(t=>e.path===t||e.path.endsWith(`/${t}`))&&s.set(e.sessionId,(s.get(e.sessionId)??0)+e.estimatedTokenWaste);let c=new Set,l=0,u=0;for(let t of e.tokenData){let e=s.get(t.sessionId);if(e){u+=e;for(let e of t.entries)c.add(C(t.sessionId,e.model||`unknown`)),l+=e.cacheReadTokens}}let d;if(u>0&&c.size>0&&l>0){let e=u/l;d={leverId:`context.repo-map-context-waste`,category:`context`,cause:`structural-prefix`,orderKey:65,ownedPools:[`cacheRead`],scopeKeys:[...c],counterfactual:{kind:`scaleTokens`,poolDeltaFrac:{cacheRead:e}},evidenceTokens:u}}let f=e=>{let t=e.symbols.length>0?` (${e.symbols.join(`, `)})`:``,n=e.reason===`config-backed`?`backs ${e.configSections.length} config section(s)`:e.reason===`stable-api`?`stable exported API, read-only`:`imported by ${e.centrality} files, read-only`;return`${e.path}${t} — ${n}, re-read across ${e.sessions} session(s), ~${e.rereadTokens.toLocaleString()} tokens`};return{id:`context.repo-map-context-waste`,category:`context`,severity:`info`,title:`Stable structural files re-read across sessions`,detail:`${n.length} stable, read-only file(s) from the repo map are re-read across sessions, re-paying ~${i.toLocaleString()} tokens. These are structural API/config/high-centrality files — pinning or referencing them once removes the repeated cost.`,action:`Reference these specific files/symbols once in CLAUDE.md (with @-imports) or cite the config section, instead of re-reading the source each session.`,...d?{reclaim:d}:{},affected:n.length,view:`context`,evidence:r.map(f),fix:{target:`CLAUDE.md`,label:`Reference stable files`,note:`Append to your project CLAUDE.md. The @-prefix imports each file so its contents load once into context instead of being re-read each session. These are stable, read-only files from the structural repo map.`,snippet:`## Stable reference files\n\nReference these stable files instead of re-reading them each session:\n${r.map(e=>`- @${e.path}${e.symbols.length>0?` — ${e.symbols.join(`, `)}`:``}`).join(`
`)}`,appliedMarkers:Jt}}}},rn=4,an={id:`workflow.native-bypass`,category:`workflow`,dataDeps:[`toolData`,`tokenData`,`liveConfig`],rule(e){let t=ge(e.toolData);if(t.totalBypass<10)return null;let n=t.categories.slice(0,4),r={grep:`Bash(grep:*)`,find:`Bash(find:*)`,cat:`Bash(cat:*)`,sed:`Bash(sed:*)`,awk:`Bash(awk:*)`},i=n.map(e=>r[e.category]).filter(e=>!!e),a=i.length>0?i:[`Bash(grep:*)`,`Bash(find:*)`,`Bash(cat:*)`];if(b(e.liveConfig?.settings,`deny`,a)||b(e.liveConfig?.settings,`ask`,a))return null;let o=he(e.toolData),s=new Map;for(let e of o)e.resultBytes>0&&s.set(e.sessionId,e.resultBytes);let c=new Set,l=0,u=0;for(let t of e.tokenData??[]){let e=s.get(t.sessionId);if(e){u+=e/rn;for(let e of t.entries)c.add(C(t.sessionId,e.model||`unknown`)),l+=e.inputTokens}}let d;if(u>0&&c.size>0&&l>0){let e=u/l;d={leverId:`workflow.native-bypass`,category:`workflow`,cause:`workflow-rework`,orderKey:30,ownedPools:[`input`],scopeKeys:[...c],counterfactual:{kind:`scaleTokens`,poolDeltaFrac:{input:e}},evidenceTokens:Math.round(u)}}return{id:`workflow.native-bypass`,category:`workflow`,severity:`info`,title:`Use native tools instead of shell equivalents`,detail:`${t.totalBypass} Bash commands re-implemented a native tool: ${n.map(e=>`${e.category} (${e.count})`).join(`, `)}.`,action:`Prefer Grep/Glob/Read/Edit over grep/find/cat/sed — they are faster, cheaper, and integrate with permissions.`,...d?{reclaim:d}:{},affected:t.totalBypass,evidence:n.map(e=>`${e.category} → ${e.nativeTool}: ${e.count}×`),view:`tools`,fix:{target:`settings.json`,label:`Add deny rules`,note:`Merge into the "permissions" object in .claude/settings.json (deep-merge the "deny" array). Use "ask" instead of "deny" if you want a prompt rather than a hard block — both steer Claude onto native Grep/Glob/Read.`,snippet:JSON.stringify({permissions:{deny:a}},null,2)}}}},on={headings:[/^##\s+Key files\b/i],bodyPhrases:[`Load these files once into context`]},sn={id:`workflow.redundant-reads`,category:`workflow`,dataDeps:[`toolData`,`tokenData`,`liveConfig`],rule(e){if(y(e.liveConfig,on))return null;let t=je(e.toolData,e.tokenData);if(t.length===0)return null;let n=t.filter(e=>e.compactions>0).length,r=ke(e.toolData,e.tokenData),i=new Map;for(let e of r.repeats)e.estimatedTokenWaste<=0||i.set(e.sessionId,(i.get(e.sessionId)??0)+e.estimatedTokenWaste);let a=new Set,o=0,s=0;for(let t of e.tokenData){let e=i.get(t.sessionId);if(e){s+=e;for(let e of t.entries)a.add(C(t.sessionId,e.model||`unknown`)),o+=e.inputTokens}}let c;if(s>0&&a.size>0&&o>0){let e=s/o;c={leverId:`workflow.redundant-reads`,category:`workflow`,cause:`workflow-rework`,orderKey:30,ownedPools:[`input`],scopeKeys:[...a],counterfactual:{kind:`scaleTokens`,poolDeltaFrac:{input:e}},evidenceTokens:s}}return{id:`workflow.redundant-reads`,category:`workflow`,severity:`info`,title:`Files re-read repeatedly within a session`,detail:`${t.length} (session, file) pair(s) re-Read the same file 3+ times${n>0?`, ${n} alongside compaction (eviction-driven)`:``}. Each re-read re-pays the token cost.`,action:`Pin frequently-needed files in CLAUDE.md or pass their contents once instead of re-reading.`,...c?{reclaim:c}:{},affected:t.length,evidence:t.slice(0,5).map(e=>`${d(e.sessionId)}, ${h(e.filePath)}, ${e.reads}×`),view:`files`,fix:{target:`CLAUDE.md`,label:`Pin files in CLAUDE.md`,note:`Append to your project CLAUDE.md. The @-prefix imports the file so its contents load once into context instead of being re-Read each time. Replace the basenames with repo-relative paths.`,snippet:`## Key files (kept in context)\n\nLoad these files once into context, then reuse that copy instead of re-reading them:\n${Array.from(new Set(t.map(e=>h(e.filePath)))).slice(0,5).map(e=>`- @${e}`).join(`
`)}`,appliedMarkers:on}}}},cn={headings:[/^##\s+Common commands\b/i],bodyPhrases:[`wrap them in a script`]},ln={id:`workflow.repeated-commands`,category:`workflow`,dataDeps:[`toolData`,`liveConfig`],rule(e){if(y(e.liveConfig,cn))return null;let t=me(e.toolData);return t.length===0?null:{id:`workflow.repeated-commands`,category:`workflow`,severity:`info`,title:`Repeated commands are candidates for automation`,detail:`${t.length} command(s) ran 3+ times within a session — repetitive manual steps that a hook, skill, or Makefile target could absorb.`,action:`Wrap the most-repeated commands in a script or a SessionStart/PostToolUse hook.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${e.totalCount}×, ${l(e.command,70)}`),view:`tools`,fix:{target:`CLAUDE.md`,label:`Document a wrapper`,note:`Append to your project CLAUDE.md. Wrap these in a script or Make target and point the entry here, so future sessions run one command instead of repeating the steps.`,snippet:`## Common commands

These were run repeatedly — wrap them in a script (e.g. \`scripts/dev.sh\`) or a Makefile target and invoke that instead of re-typing the steps:

`+t.slice(0,3).map(e=>`- \`${l(e.command,70)}\``).join(`
`),appliedMarkers:cn}}}},un={id:`workflow.file-churn`,category:`workflow`,dataDeps:[`toolData`],rule(e){let t=Ae(e.toolData).filter(e=>e.churn>=15);return t.length===0?null:{id:`workflow.file-churn`,category:`workflow`,severity:`info`,title:`High-churn files`,detail:`${t.length} file(s) were edited/written 15+ times. Heavy churn can signal an unclear interface or repeated trial-and-error.`,action:`Review whether these files need refactoring or clearer up-front specs to reduce rework.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${h(e.filePath)}, ${e.churn} edits / ${e.sessions} sessions`),view:`files`}}},dn={id:`workflow.assistant-refusal-rate`,category:`workflow`,dataDeps:[`assistantFeatures`],rule(e){let t=e.assistantFeatures??[];if(t.length===0)return null;let n=0,r=0;for(let e of t)n+=e.assistantTurnCount,r+=e.refusalCount;if(n<50)return null;let i=r/n;return i<.15?null:{id:`workflow.assistant-refusal-rate`,category:`workflow`,severity:`info`,title:`Assistant frequently course-corrects`,detail:`${Math.round(i*100)}% of assistant turns (${r} of ${n}) contain a refusal or concession ("I cannot", "I apologize", "you're right") — often a sign the prompt or project context left it guessing.`,action:`Tighten recurring instructions in CLAUDE.md (constraints, file locations, conventions) so the assistant needs fewer corrections.`,affected:r}}},M=30,fn=1440*60*1e3;function pn(e,t){let n=new Map(t.map(e=>[e.sessionId,e.startTime])),r=[];for(let t of e){let e=n.get(t.sessionId);e!=null&&r.push({startTime:e,agents:N(t.agents),skills:N(t.skills),commands:N(t.commands??{}),mcpServers:N(t.mcpServers)})}return r}function N(e){let t={};for(let[n,r]of Object.entries(e))r&&typeof r.invocations==`number`&&(t[n]=r.invocations);return t}function P(e,t,n,r){let i=null,a=0,o=0;for(let s of t){let t=r(s,e);t<=0||(a+=t,s.startTime>=n&&(o+=t),(i==null||s.startTime>i)&&(i=s.startTime))}return{lastSeen:i,lifetimeCount:a,windowCount:o}}function mn(e,t){if(e.length===0)return 1/0;let n=t;for(let t of e)t.startTime>0&&t.startTime<n&&(n=t.startTime);return Math.max(0,(t-n)/fn)}function F(e){let{resourceType:t,resourceId:n,scope:r,summary:i,hedge:a}=e;return{id:`${t}.unused:${n}${r.kind===`project`?`@${r.project}`:``}`,resourceType:t,resourceId:n,scope:r,state:`unused`,lastSeen:i.lastSeen,lifetimeCount:i.lifetimeCount,windowCount:i.windowCount,windowDays:M,...a?{hedge:a}:{}}}function hn(e,t,n,r){let i=[];for(let a of e){let e=P(a.id,t,n,(e,t)=>e.mcpServers[t]??0);if(e.windowCount>0)continue;let o=a.scope===`global`?{kind:`global`}:{kind:`project`,project:a.enabledByProjects?.[0]??`(unknown)`};i.push(F({resourceType:`mcpServer`,resourceId:a.id,scope:o,summary:e,hedge:r}))}return i}function gn(e,t,n,r){let i=[];for(let a of e){let e=a.bundled;if(!e)continue;let o=e.skills??[],s=e.agents??[];if(o.length===0&&s.length===0)continue;let c=null,l=0,u=0;for(let e of o){let r=P(e,t,n,(e,t)=>e.skills[t]??0);l+=r.lifetimeCount,u+=r.windowCount,r.lastSeen!=null&&(c==null||r.lastSeen>c)&&(c=r.lastSeen)}for(let e of s){let r=P(e,t,n,(e,t)=>e.agents[t]??0);l+=r.lifetimeCount,u+=r.windowCount,r.lastSeen!=null&&(c==null||r.lastSeen>c)&&(c=r.lastSeen)}u>0||i.push(F({resourceType:`plugin`,resourceId:a.id,scope:a.scope===`project`?{kind:`project`,project:a.installPath||`(unknown)`}:{kind:`global`},summary:{lastSeen:c,lifetimeCount:l,windowCount:u},hedge:r}))}return i}function I(e){let t=e.liveConfig;if(!t)return[];let n=e.now??Date.now(),r=n-M*fn,i=mn(e.sessions,n)<M?`window-shorter-than-threshold`:void 0,a=pn(e.attribution,e.sessions),o=[];for(let e of t.skills){let t=P(e.id,a,r,(e,t)=>e.skills[t]??0);t.windowCount>0||o.push(F({resourceType:`skill`,resourceId:e.id,scope:{kind:`global`},summary:t,hedge:i}))}for(let e of t.subagents){let t=P(e.id,a,r,(e,t)=>e.agents[t]??0);t.windowCount>0||o.push(F({resourceType:`subagent`,resourceId:e.id,scope:{kind:`global`},summary:t,hedge:i}))}for(let e of t.commands){let t=P(e.id,a,r,(e,t)=>e.commands[t]??0);t.windowCount>0||o.push(F({resourceType:`command`,resourceId:e.id,scope:{kind:`global`},summary:t,hedge:i}))}return o.push(...hn(t.mcpServers,a,r,i)),o.push(...gn(t.plugins,a,r,i)),yn(o)}var L=[`skill`,`subagent`,`command`,`mcpServer`,`plugin`],_n=Object.fromEntries(L.map((e,t)=>[e,t]));function vn(e){let t=Object.fromEntries(L.map(e=>[e,[]]));for(let n of e)(t[n.resourceType]??=[]).push(n);return t}function yn(e){return e.slice().sort((e,t)=>{let n=_n[e.resourceType]-_n[t.resourceType];if(n!==0)return n;let r=e.lastSeen??-1,i=t.lastSeen??-1;return r===i?e.resourceId.localeCompare(t.resourceId):r-i})}var bn={skill:`Skills`,subagent:`Subagents`,command:`Commands`,mcpServer:`MCP servers`,plugin:`Plugins`},xn=3,Sn={id:`workflow.unused-installed-skills`,category:`workflow`,dataDeps:[`liveConfig`,`attribution`,`sessions`],rule(e,t){if(!e.liveConfig)return null;let n=I({liveConfig:e.liveConfig,attribution:e.attribution??[],sessions:e.sessions.map(e=>({sessionId:e.sessionId,startTime:e.startTime})),now:t}).filter(e=>e.resourceType===`skill`&&e.windowCount===0);return n.length<xn?null:{id:`workflow.unused-installed-skills`,category:`workflow`,severity:`info`,title:`Installed skills unused in the last 30 days`,detail:`${n.length} installed skill(s) had zero invocations in the last 30 days; unused skills add selection ambiguity when Claude picks which to invoke.`,action:`Remove unused skills from ~/.claude/skills/ to keep skill-selection clean.`,affected:n.length,evidence:n.slice(0,5).map(e=>e.resourceId)}}},Cn=3,wn={id:`workflow.unused-installed-subagents`,category:`workflow`,dataDeps:[`liveConfig`,`attribution`,`sessions`],rule(e,t){if(!e.liveConfig)return null;let n=I({liveConfig:e.liveConfig,attribution:e.attribution??[],sessions:e.sessions.map(e=>({sessionId:e.sessionId,startTime:e.startTime})),now:t}).filter(e=>e.resourceType===`subagent`&&e.windowCount===0);return n.length<Cn?null:{id:`workflow.unused-installed-subagents`,category:`workflow`,severity:`info`,title:`Installed subagents unused in the last 30 days`,detail:`${n.length} installed subagent(s) had zero invocations in the last 30 days; unused subagents add selection ambiguity when Claude picks which to invoke.`,action:`Remove unused subagents from ~/.claude/agents/ to keep agent-selection clean.`,affected:n.length,evidence:n.slice(0,5).map(e=>e.resourceId)}}},Tn=3,En={id:`workflow.unused-installed-commands`,category:`workflow`,dataDeps:[`liveConfig`,`attribution`,`sessions`],rule(e,t){if(!e.liveConfig)return null;let n=I({liveConfig:e.liveConfig,attribution:e.attribution??[],sessions:e.sessions.map(e=>({sessionId:e.sessionId,startTime:e.startTime})),now:t}).filter(e=>e.resourceType===`command`&&e.windowCount===0);return n.length<Tn?null:{id:`workflow.unused-installed-commands`,category:`workflow`,severity:`info`,title:`Installed slash-commands unused in the last 30 days`,detail:`${n.length} installed slash-command(s) had zero invocations in the last 30 days; unused commands clutter the slash menu and add selection ambiguity.`,action:`Remove unused commands from ~/.claude/commands/ to keep the slash menu clean.`,affected:n.length,evidence:n.slice(0,5).map(e=>e.resourceId)}}},Dn=new Set([`Edit`,`Write`,`MultiEdit`,`NotebookEdit`]),On=10,kn=.1;function An(e){let t=e?.hooks?.PreToolUse;return Array.isArray(t)?t.some(e=>/\bEdit\b|\bWrite\b/.test(typeof e?.matcher==`string`?e.matcher:``)):!1}var jn={id:`workflow.tool-undo-rate`,category:`workflow`,dataDeps:[`toolData`,`apiErrors`,`timelines`,`liveConfig`],rule(e){if(An(e.liveConfig?.settings))return null;let t=ve(e.toolData,e.apiErrors,e.timelines??[]).filter(e=>Dn.has(e.tool)&&e.invocations>=On&&e.immediatelyFollowedByUndo/e.invocations>=kn);if(t.length===0)return null;let n=e=>e.immediatelyFollowedByUndo/e.invocations;t.sort((e,t)=>n(t)-n(e));let r=t[0];return{id:`workflow.tool-undo-rate`,category:`workflow`,severity:`info`,title:`Reduce immediate rollbacks on file-editing tools`,detail:`${r.tool} edits are rolled back (git checkout/restore/revert immediately after) ${Math.round(n(r)*100)}% of the time over ${r.invocations} invocations — each undo cycle is 2+ wasted round-trips.`,action:`Add a PreToolUse hook that runs a quick lint/typecheck before edits commit, catching errors before they need a rollback.`,affected:r.immediatelyFollowedByUndo,evidence:t.slice(0,5).map(e=>`${e.tool}: ${Math.round(n(e)*100)}% undo over ${e.invocations}`),view:`tools`,fix:{target:`settings.json`,fixKind:`illustrative`,label:`Add a pre-edit check hook`,note:`Merge into .claude/settings.json hooks. Swap the command for your real lint/typecheck so broken edits are caught before they need rolling back.`,snippet:`{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "npm run -s lint" }
        ]
      }
    ]
  }
}`}}}},Mn=10,Nn=.4,Pn={id:`workflow.low-tool-effectiveness`,category:`workflow`,dataDeps:[`toolData`,`apiErrors`,`timelines`,`liveConfig`],rule(e){if(_(e.liveConfig?.settings))return null;let t=ve(e.toolData,e.apiErrors,e.timelines??[]).filter(e=>e.effectivenessScore<Nn&&e.invocations>=Mn);if(t.length===0)return null;t.sort((e,t)=>e.effectivenessScore-t.effectivenessScore);let n=t[0];return{id:`workflow.low-tool-effectiveness`,category:`workflow`,severity:`info`,title:`Fix tools with persistently low effectiveness`,detail:`Tool ${n.tool} has an effectiveness score of ${n.effectivenessScore.toFixed(2)} (below ${Nn}) over ${n.invocations} invocations — error/undo/retry signals outweigh forward motion.`,action:`Add a PostToolUse hook that validates the result (exit code, path-exists, typecheck) for the flagged tool so errors surface before the next turn.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${e.tool}: score ${e.effectivenessScore.toFixed(2)} over ${e.invocations}`),view:`tools`,fix:{target:`hook`,fixKind:`illustrative`,label:`Validate the low-scoring tool`,note:`Merge into .claude/settings.json hooks. Validates ${n.tool} results so errors surface immediately; swap the command for your real check.`,snippet:`{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "${n.tool}",
        "hooks": [
          { "type": "command", "command": "npm run -s typecheck" }
        ]
      }
    ]
  }
}`}}}};function R(e){return e.tokenDeltaCount>0?e.tokenDeltaSum/e.tokenDeltaCount:null}function z(e){return e.costDeltaCount>0?e.costDeltaSum/e.costDeltaCount:null}function Fn(e){let t=z(e);if(t!==null)return t<0;let n=R(e);return n===null?null:n<0}var In=5,Ln=3,Rn=.6,zn=3,Bn={model:{label:`cheaper model`,adopt:`default to the cheaper model tier for this class of task`},reasoning:{label:`different reasoning effort`,adopt:`adjust extended-thinking depth to match what the experiments favoured`},prompt:{label:`structured prompt`,adopt:`frame these tasks with explicit steps / acceptance criteria up front`},skills:{label:`a dedicated skill`,adopt:`route this kind of task through the matching skill (e.g. /diagnose, /tdd)`},plugins:{label:`a plugin workflow`,adopt:`run these through the plugin workflow instead of ad-hoc steps`},mcp:{label:`an MCP server`,adopt:`prefer the MCP path over the native one for these tasks`},subagents:{label:`subagent fan-out`,adopt:`parallelise discovery with subagents / Explore for these tasks`},tools:{label:`a different tool strategy`,adopt:`switch the default tool strategy for these tasks`},mode:{label:`plan-first mode`,adopt:`plan before acting on these tasks`},context:{label:`fresh context`,adopt:`attempt these from a clean context rather than carrying full session state`}};function Vn(e){return Bn[e]??{label:`the "${e}" variation`,adopt:`adopt the "${e}" variation as the default`}}function Hn(e){return e.shadowWins+e.mainWins}var Un={id:`workflow.shadow-axis-wins`,category:`workflow`,dataDeps:[`shadowCalls`],rule(e){let t=e.shadowCalls;if(!t||t.total===0||t.byAxis.length===0)return null;let n=[];for(let e of t.byAxis){if(e.samples<In)continue;let t=Hn(e);if(t<Ln)continue;let r=e.shadowWins/t;if(r<Rn)continue;let i=Fn(e)===!0,a=z(e),o=R(e),s=e.shadowWins+e.liveShadowWins*2+(i?2:0);n.push({a:e,winRate:r,cheaper:i,costDelta:a,tokenDelta:o,score:s})}if(n.length===0)return null;n.sort((e,t)=>t.score-e.score);let r=n[0],i=r.a,a=Vn(i.axis),o=i.liveShadowWins>=zn,s=o?`warning`:`info`,c=Math.round(r.winRate*100),l=`${i.live} live + ${i.replay} replay`,u=r.cheaper?r.costDelta===null?` and used ~${Math.abs(Math.round(r.tokenDelta))} fewer tokens on average`:` and cost ~$${Math.abs(r.costDelta).toFixed(2)} less on average`:``,d=o?`${i.liveShadowWins} of these were confirmed in live, in-the-loop runs`:`evidence is mostly from replay (cold-start caveat), so treat this as a lead to confirm live`,f=n.slice(1,3).map(e=>`${Vn(e.a.axis).label} (${Math.round(e.winRate*100)}% over ${e.a.samples})`);return{id:`workflow.shadow-axis-wins`,category:`workflow`,severity:s,title:`Adopt ${a.label} — it out-performed your default in shadow tests`,detail:`Across ${i.samples} shadow experiment(s) on the "${i.axis}" axis (${l}), the variation won ${i.shadowWins}/${Hn(i)} decided comparisons (${c}%)${u}. ${d}.`,action:`For this class of task, ${a.adopt}. Re-run a few live shadows to confirm before making it your standing default.`,affected:i.samples,view:`recommendations`,evidence:[`${i.axis}: shadow ${i.shadowWins} / main ${i.mainWins} / tie ${i.ties} over ${i.samples} (${l})`,r.costDelta===null?r.tokenDelta===null?`no paired cost/token data`:`avg token delta (shadow−main): ${Math.round(r.tokenDelta)}`:`avg $ delta (shadow−main): $${r.costDelta.toFixed(2)}`,...f.length?[`other promising axes: ${f.join(`, `)}`]:[]],fix:{target:`CLAUDE.md`,label:`Adopt the winning variation`,note:`Shadow-calls evidence (epic #513). Add a standing note so this becomes the default for this kind of task; keep shadowing to catch regressions.`,snippet:`## Default approach (from shadow-calls #513)\n\nFor this class of task, ${a.adopt} — shadow experiments on the "${i.axis}" axis won ${c}% of ${i.samples} comparisons${r.cheaper?` at lower token cost`:``}. Revisit if live shadows stop favouring it.`,appliedMarkers:{headings:[/^##\s+default approach/i],bodyPhrases:[`Revisit if live shadows stop favouring it`]}}}}},Wn=5,Gn=3,Kn=.6,qn={model:[`cost.legacy-model-overpay`,`cost.unknown-model`],tools:[`workflow.native-bypass`],context:[`context.over-window`,`context.repeated-compactions`],reasoning:[],prompt:[],skills:[],plugins:[],mcp:[],subagents:[],mode:[]},Jn={reasoning:`context`,prompt:`workflow`,skills:`workflow`,plugins:`workflow`,mcp:`workflow`,subagents:`workflow`,mode:`workflow`};function B(e){return e.shadowWins+e.mainWins}var Yn={id:`workflow.uncovered-shadow-axis`,category:`workflow`,dataDeps:[`shadowCalls`],rule(e){let t=e.shadowCalls;if(!t||t.total===0)return null;let n=t.byAxis.filter(e=>{if(e.samples<Wn)return!1;let t=B(e);if(t<Gn||e.shadowWins/t<Kn)return!1;let n=qn[e.axis];return n!==void 0&&n.length===0}).sort((e,t)=>t.shadowWins-e.shadowWins);if(n.length===0)return null;let r=n[0],i=B(r),a=Math.round(r.shadowWins/i*100),o=`${Jn[r.axis]??`workflow`}.shadow-${r.axis}`,s=z(r),c=R(r),l=n.map(e=>e.axis).join(`, `),u=`New recommendation class to add: "${r.axis}" wins in shadow tests but no rule covers it`,d=`Shadow-calls (#513) show the "${r.axis}" variation winning ${r.shadowWins}/${i} (${a}%) over ${r.samples} experiments, but no detector encodes that pattern. Propose a dedicated detector \\\`${o}\\\`.`;return{id:`workflow.uncovered-shadow-axis`,category:`workflow`,severity:`info`,title:u,detail:`Shadow experiments favour the "${r.axis}" axis (${r.shadowWins}/${i} = ${a}% over ${r.samples}), but the recommendation catalog has no rule for it`+(s!==null&&s<0?`, and it cost ~$${Math.abs(s).toFixed(2)} less on average`:c!==null&&c<0?`, and it ran cheaper (~${Math.abs(Math.round(c))} fewer tokens avg)`:``)+`. That's a candidate for a new recommendation class. Uncovered axes with wins: ${l}.`,action:`Propose a dedicated detector (e.g. \`${o}\`) so this pattern becomes a permanent, verifiable recommendation rather than living only in the ledger. File it as a backlog issue (per the engine-gap feedback loop).`,affected:n.length,view:`recommendations`,evidence:n.map(e=>`${e.axis}: shadow ${e.shadowWins}/${B(e)} (${e.live}L+${e.replay}R), no covering rule → suggest ${Jn[e.axis]??`workflow`}.shadow-${e.axis}`),fix:{target:`command`,label:`File a new-rule proposal`,note:`Discovery proposal (epic #513 / ADR 0002). Dedup against open issues titled "[shadow-discovery]" before filing.`,snippet:`gh issue create --repo shpwrck/claude-history-dashboard --label backlog --label enhancement \\\n  --title "[shadow-discovery] New detector for winning axis: ${r.axis}" \\\n  --body "${d}"`}}}},Xn=1440*60*1e3,Zn={id:`workflow.abandoned-tasks`,category:`workflow`,dataDeps:[`tasks`],rule(e,t){let n=e.tasks??[];if(!n.length)return null;let r=7*Xn,i=e=>e.status===`pending`||e.status===`in_progress`,a=new Map;for(let e of n){let t=a.get(e.sessionId)??[];t.push(e),a.set(e.sessionId,t)}let o=[];for(let[e,n]of a){let a=n.filter(i);if(!a.length)continue;let s=t-Math.max(...n.map(e=>e.mtimeMs));if(s<r)continue;let c=Math.round(s/Xn);o.push({sessionId:e,daysSinceActive:c,count:a.length,subjects:a.map(e=>e.subject)})}if(!o.length)return null;o.sort((e,t)=>t.count-e.count);let s=o.reduce((e,t)=>e+t.count,0),c=o[0],l=o.slice(0,5).map(e=>`${d(e.sessionId)}: ${e.count} open task(s), ${e.daysSinceActive}d idle`),u=o.slice(0,3).map(e=>`cat ~/.claude/tasks/${e.sessionId}/*.json | jq 'select(.status!="completed") | .subject'`).join(`
`);return{id:`workflow.abandoned-tasks`,category:`workflow`,severity:`warning`,title:`Abandoned open tasks in cold sessions`,detail:`${s} open task(s) across ${o.length} session(s) idle >= 7 days — work that was never PR'd or formally dropped. Worst: ${d(c.sessionId)} with ${c.count} task(s) idle ${c.daysSinceActive}d.`,action:`Re-open, reassign, or formally close these tasks. Use the fix snippet to inspect what is still open per session.`,affected:s,evidence:l,fix:{target:`command`,label:`Inspect abandoned tasks`,note:`Run per-session to list tasks never completed. Close or reassign each one.`,snippet:u}}}},Qn={id:`workflow.blocked-task-pileup`,category:`workflow`,dataDeps:[`tasks`],rule(e){let t=e.tasks??[];if(!t.length)return null;let n=[],r=new Map;for(let e of t){let t=r.get(e.sessionId)??[];t.push(e),r.set(e.sessionId,t)}for(let[e,t]of r){let r=new Map(t.map(e=>[e.id,e])),i=new Map;for(let e of t)if(e.status!==`completed`)for(let t of e.blockedBy){let n=r.get(t);if(!n||n.status===`completed`)continue;let a=i.get(t)??[];a.push(e),i.set(t,a)}for(let[t,a]of i){if(a.length<2)continue;let i=r.get(t);n.push({sessionId:e,rootId:t,rootSubject:i.subject,blockedCount:a.length,blockedSubjects:a.map(e=>e.subject)})}}if(!n.length)return null;n.sort((e,t)=>t.blockedCount-e.blockedCount);let i=n[0],a=n.reduce((e,t)=>e+t.blockedCount,0),o=n.slice(0,5).map(e=>`${d(e.sessionId)}: ${e.blockedCount} task(s) blocked behind "${e.rootSubject}"`),s=n.slice(0,3).map(e=>`# Unblock: "${e.rootSubject}" (session ${d(e.sessionId)})\n# Assign an owner or split the root; ${e.blockedCount} downstream task(s) then unblock:\n`+e.blockedSubjects.map(e=>`#   - ${e}`).join(`
`)).join(`

`);return{id:`workflow.blocked-task-pileup`,category:`workflow`,severity:`warning`,title:`Blocked task pileup — unfinished DAG root holding up downstream work`,detail:`${a} open task(s) across ${n.length} pileup(s) are stalled behind an unfinished root. Worst: ${i.blockedCount} task(s) waiting on "${i.rootSubject}" in session ${d(i.sessionId)}.`,action:`Assign an owner to the root task first, or split it into smaller steps. Until the root is resolved, the blocked tasks are dead weight.`,affected:a,evidence:o,fix:{target:`command`,label:`Identify and unblock root tasks`,note:`Review each root task. Assign an owner or split it; downstream tasks unblock automatically.`,snippet:s}}}};function V(e){return typeof e==`string`&&e.trim().length>0}function H(e,t){let n=t.trim();if(!n)return``;switch(e){case`email`:case`username`:case`task-owner`:case`git-author`:case`session-user`:return n.toLowerCase()}}function $n(e){return`${e.kind}:${H(e.kind,e.value)}`}function er(e){if(!V(e.value))return null;let t=H(e.kind,e.value);return t?{kind:e.kind,value:t,...e.source?{source:e.source}:{}}:null}function tr(e){return[...e.aliases??[],...V(e.email)?[{kind:`email`,value:e.email,source:`contributor.email`}]:[]]}function nr(e){let t=new Map;if(e?.contributors?.length){for(let n of e.contributors)if(V(n.id))for(let e of tr(n)){let r=er(e);if(!r)continue;let i=$n(r),a=t.get(i)??new Map;a.set(n.id,n),t.set(i,a)}}return n=>{let r=er(n);if(!r||!e?.contributors?.length)return{status:`unknown`,alias:n};let i=t.get($n(r));return!i||i.size===0?{status:`unknown`,alias:r}:i.size>1?{status:`ambiguous`,alias:r,contributorIds:[...i.keys()].sort()}:{status:`known`,alias:r,contributor:[...i.values()][0]}}}var rr=.6;function ir(e){return(e.status===`pending`||e.status===`in_progress`)&&e.owner.trim().length>0}function U(e){return Math.round(e*100)}function ar(e){if(!(!Number.isFinite(e)||e<=0))return new Date(e).toISOString().slice(0,10)}function or(e){return e.subject.trim()||e.activeForm.trim()||e.id}function sr(e,t){let n=t.get(e.sessionId);return(n?.projectShort||n?.project)?.trim()||null}function cr(e){return[...e.entries()].sort((e,t)=>t[1]-e[1]||e[0].localeCompare(t[0])).slice(0,4).map(([e,t])=>`${e} (${t})`)}function lr(e,t){let n=t({kind:`task-owner`,value:e,source:`~/.claude/tasks owner`});return n.status===`known`?{key:`contributor:${n.contributor.id}`,label:n.contributor.displayName||n.contributor.id,identityId:n.contributor.id,identityStatus:`known`}:n.status===`ambiguous`?{key:`ambiguous:${e}`,label:e,identityStatus:`ambiguous`}:{key:`raw:${e}`,label:e,identityStatus:`unknown`}}function ur(e){let t=[...e.rawOwners].sort();return t.length>1?` via aliases ${t.join(`, `)}`:``}var dr={id:`workflow.owner-concentration`,category:`workflow`,dataDeps:[`tasks`,`sessions`,`organizationIdentity`],rule(e,t){let n=e.tasks??[];if(!n.length)return null;let r=new Map(e.sessions.map(e=>[e.sessionId,e])),i=nr(e.organizationIdentity),a=new Map,o=0,s=0;for(let e of n){if(!ir(e))continue;let t=e.owner.trim(),n=lr(t,i),c=a.get(n.key)??{key:n.key,owner:n.label,tasks:[],projectCounts:new Map,rawOwners:new Set,...n.identityId?{identityId:n.identityId}:{},identityStatus:n.identityStatus};c.tasks.push(e),c.rawOwners.add(t);let l=sr(e,r);l&&c.projectCounts.set(l,(c.projectCounts.get(l)??0)+1),a.set(n.key,c),o+=1,s=Math.max(s,e.mtimeMs)}if(o<6||a.size<2)return null;let c=[...a.values()].sort((e,t)=>t.tasks.length-e.tasks.length||e.owner.localeCompare(t.owner)),l=c[0],u=l.tasks.length/o;if(l.tasks.length<4||u<.6)return null;let f=U(u),p=cr(l.projectCounts),m=p.length?` Top projects for that owner: ${p.join(`, `)}.`:``,h=[`${l.owner}: ${l.tasks.length}/${o} open owned task(s) (${f}%)${ur(l)}`,...c.slice(1,4).map(e=>`${e.owner}: ${e.tasks.length}/${o} open owned task(s) (${U(e.tasks.length/o)}%)${ur(e)}`),...l.tasks.slice(0,4).map(e=>{let t=sr(e,r),n=t?` (${t})`:``;return`${d(e.sessionId)}: ${or(e)}${n}`})],g=ar(s),_=g&&Number.isFinite(t)&&t>0?t-s>336*60*60*1e3:void 0;return{id:`workflow.owner-concentration`,category:`workflow`,severity:u>=.75||l.tasks.length>=8?`warning`:`info`,title:`Open task ownership is concentrated on ${l.owner}`,detail:`${l.owner} owns ${l.tasks.length} of ${o} open owned task(s) (${f}%) across ${a.size} owner(s).${m}`,action:`Rebalance the queue before this becomes a review, support, or handoff bottleneck. Pair on the oldest/highest-risk tasks, or reassign clear follow-up work to another owner.`,affected:l.tasks.length,view:`tasks`,evidence:h,provenance:{observations:[{claim:`${o} open tasks have a non-empty owner`,source:`~/.claude/tasks/`,field:`status / owner`,value:o},{claim:`${l.owner} owns ${l.tasks.length} of those open tasks`,source:`~/.claude/tasks/`,field:`owner / status`,value:l.tasks.length},{claim:`${a.size} distinct owner group(s) have open tasks after explicit aliases are applied when present`,source:`~/.claude/tasks/ + organizationIdentity`,field:`owner / status / contributors[].aliases[kind=task-owner]`,value:a.size},...l.identityStatus===`known`&&l.identityId?[{claim:`${l.owner} matched explicit contributor id ${l.identityId}`,source:`organizationIdentity`,field:`contributors[].aliases[kind=task-owner]`,value:l.identityId}]:[]],inference:`${l.owner}'s ${f}% share is above the ${U(rr)}% multi-owner concentration threshold, so the queue has a single-owner bottleneck risk.`,...g?{asOf:g}:{},..._===void 0?{}:{stale:_}}}}},W=3600*1e3;function G(e){return typeof e==`string`&&e.trim().length>0}function fr(e){if(!G(e))return null;let t=Date.parse(e);return Number.isFinite(t)?t:null}function pr(e){if(!(e==null||!Number.isFinite(e)||e<=0))return new Date(e).toISOString().slice(0,10)}function mr(e){return!G(e.reviewerId)||e.state!==`pending`||G(e.respondedAt)?!1:e.pullRequestState==null||e.pullRequestState===`open`}function K(e){let t=Math.max(1,Math.floor(e/W));if(t<48)return`${t}h`;let n=Math.floor(t/24),r=t%24;return r>0?`${n}d ${r}h`:`${n}d`}function q(e){let t=e.repository.trim()||`unknown-repo`,n=Number.isFinite(e.pullRequestNumber)?`#${e.pullRequestNumber}`:`#?`,r=e.pullRequestTitle?.trim();return r?`${t}${n} ${r}`:`${t}${n}`}function hr(e,t){let n=e.reviewerId.trim(),r=t({kind:`username`,value:n,source:`reviewEvents reviewerId`});if(r.status===`known`)return{key:`contributor:${r.contributor.id}`,label:r.contributor.displayName||r.contributor.id,identityId:r.contributor.id,identityStatus:`known`};let i=e.reviewerDisplayName?.trim()||n;return r.status===`ambiguous`?{key:`ambiguous:${H(`username`,n)}`,label:i,identityStatus:`ambiguous`}:{key:`raw:${H(`username`,n)}`,label:i,identityStatus:`unknown`}}function gr(e){let t=[...e.rawReviewerIds].sort();return t.length>1?` via aliases ${t.join(`, `)}`:``}var _r={id:`workflow.review-bottleneck`,category:`workflow`,dataDeps:[`reviewEvents`,`organizationIdentity`],rule(e,t){if(!Number.isFinite(t)||t<=0)return null;let n=e.reviewEvents,r=n?.reviewRequests??[];if(!r.length)return null;let i=48*W,a=[];for(let e of r){if(!mr(e))continue;let n=fr(e.requestedAt);n==null||n>t||a.push({request:e,ageMs:t-n})}if(!a.length)return null;let o=a.filter(e=>e.ageMs>=i);if(o.length<3)return null;let s=nr(e.organizationIdentity),c=new Map;for(let e of o){let t=hr(e.request,s),n=c.get(t.key)??{key:t.key,reviewer:t.label,requests:[],rawReviewerIds:new Set,...t.identityId?{identityId:t.identityId}:{},identityStatus:t.identityStatus};n.requests.push(e),n.rawReviewerIds.add(e.request.reviewerId.trim()),c.set(t.key,n)}let l=[...c.values()].sort((e,t)=>{let n=Math.max(...e.requests.map(e=>e.ageMs)),r=Math.max(...t.requests.map(e=>e.ageMs));return t.requests.length-e.requests.length||r-n||e.reviewer.localeCompare(t.reviewer)}),u=l[0];if(!u||u.requests.length<2)return null;u.requests.sort((e,t)=>t.ageMs-e.ageMs||q(e.request).localeCompare(q(t.request)));let d=u.requests[0]?.ageMs??0,f=u.requests.length>=4||d>=120*W?`warning`:`info`,p=n?.source?.trim()||`reviewEvents`,m=pr(fr(n?.generatedAt)),h=[`${u.reviewer}: ${u.requests.length}/${o.length} stale pending review request(s), oldest ${K(d)}${gr(u)}`,...l.slice(1,4).map(e=>{let t=Math.max(...e.requests.map(e=>e.ageMs));return`${e.reviewer}: ${e.requests.length}/${o.length} stale pending review request(s), oldest ${K(t)}${gr(e)}`}),...u.requests.slice(0,4).map(e=>{let t=e.request.pullRequestUrl?.trim(),n=t?` (${t})`:``;return`${q(e.request)}: requested ${K(e.ageMs)} ago${n}`})];return{id:`workflow.review-bottleneck`,category:`workflow`,severity:f,title:`PR review queue is bottlenecked on ${u.reviewer}`,detail:`${u.reviewer} has ${u.requests.length} stale pending PR review request(s) older than 48h. Across the source, ${o.length} of ${a.length} pending request(s) are stale.`,action:`Reassign or pair on the oldest reviews, then add a reviewer rotation or escalation rule before stale requests pile up behind one person.`,affected:u.requests.length,evidence:h,provenance:{observations:[{claim:`${a.length} PR review request(s) are pending`,source:p,field:`reviewRequests[state=pending]`,value:a.length},{claim:`${o.length} pending PR review request(s) are older than 48h`,source:p,field:`reviewRequests.requestedAt`,value:o.length},{claim:`${u.reviewer} has ${u.requests.length} stale pending PR review request(s)`,source:p,field:`reviewRequests.reviewerId / requestedAt`,value:u.requests.length},...u.identityStatus===`known`&&u.identityId?[{claim:`${u.reviewer} matched explicit contributor id ${u.identityId}`,source:`organizationIdentity`,field:`contributors[].aliases[kind=username]`,value:u.identityId}]:[]],inference:`${u.reviewer}'s stale review queue is at least 2 requests and the oldest pending request has waited ${K(d)}, so review handoff latency is concentrated on one reviewer.`,...m?{asOf:m}:{}}}}},vr=/\b(abort|error|fail)/i,yr=/\b(error|fail)/i;function br(e){return vr.test(e.status)?!0:e.agents.some(e=>e.state!=null&&yr.test(e.state))}var xr={id:`workflow.failed-workflow-runs`,category:`workflow`,dataDeps:[`workflows`],rule(e){let t=e.workflows??[];if(!t.length)return null;let n=t.filter(br);if(!n.length)return null;let r=n.slice(0,5).map(e=>{let t=e.agents.filter(e=>e.state!=null&&yr.test(e.state)).length,n=t>0?`${t} agent(s) errored`:`status ${e.status}`;return`${e.workflowName} (${e.runId}): ${n}`});return{id:`workflow.failed-workflow-runs`,category:`workflow`,severity:`warning`,title:`Workflow runs failed or aborted`,detail:`${n.length} of ${t.length} Workflow-tool run(s) failed, aborted, or left an agent in an error state — wasted fan-out cost with no completed result.`,action:`Review the failed runs in the Workflows view; fix the failing stage or add a guard so the fan-out does not burn cost on a run that cannot complete.`,affected:n.length,view:`workflows`,evidence:r}}},Sr=4,Cr=3,wr=2e5;function Tr(e){let t=[...e].sort((e,t)=>e-t),n=Math.floor(t.length/2);return t.length%2?t[n]:(t[n-1]+t[n])/2}var Er={id:`workflow.runaway-workflow-cost`,category:`workflow`,dataDeps:[`workflows`,`tokenData`],rule(e){let t=(e.workflows??[]).filter(e=>typeof e.totalTokens==`number`);if(t.length<Sr)return null;let n=Tr(t.map(e=>e.totalTokens));if(n<=0)return null;let r=t.filter(e=>e.totalTokens>Cr*n&&e.totalTokens>wr).sort((e,t)=>t.totalTokens-e.totalTokens);if(!r.length)return null;let i=r.slice(0,5).map(e=>`${e.workflowName} (${e.runId}): ${e.totalTokens.toLocaleString()} tokens, ${(e.totalTokens/n).toFixed(1)}x median${e.agentCount==null?``:`, ${e.agentCount} agents`}`),a=new Set;for(let e of r){if((e.agentCount??0)<=1||!e.sessionId)continue;let t=e.defaultModel||`unknown`;a.add(C(e.sessionId,t))}let o;return a.size>0&&(o={leverId:`workflow.runaway-workflow-cost`,category:`workflow`,cause:`workflow-rework`,orderKey:30,ownedPools:[`cacheWrite5m`],scopeKeys:[...a],counterfactual:{kind:`convertRate`,rateFrom:`cacheWrite5m`,rateTo:`cacheRead`},evidenceTokens:0}),{id:`workflow.runaway-workflow-cost`,category:`workflow`,severity:`warning`,title:`Workflow run with runaway fan-out cost`,detail:`${r.length} Workflow-tool run(s) burned more than ${Cr}x the median run's token spend (median ${Math.round(n).toLocaleString()} tokens) — a sign of an unbounded loop or an over-wide parallel stage.`,action:`Cap the fan-out (concurrency / max agents) or add a budget guard so a single run cannot dominate spend; review the outlier runs in the Workflows view.`,...o?{reclaim:o}:{},affected:r.length,view:`workflows`,evidence:i}}},Dr=3,Or=10,kr={id:`workflow.rework-signature`,category:`workflow`,dataDeps:[`sessions`],rule(e){let t=e.fileHistory??[];if(t.length<Dr)return null;let n=[...t].sort((e,t)=>t.reworkScore-e.reworkScore)[0];if(n.reworkScore<Or)return null;let r=[...t].sort((e,t)=>t.reworkScore-e.reworkScore).slice(0,5);return{id:`workflow.rework-signature`,category:`workflow`,severity:n.burstRate>=3?`warning`:`info`,title:`High file-snapshot churn signals retry storms`,detail:`Session ${d(n.sessionId)} has reworkScore ${n.reworkScore} (${n.churn} pre-edit checkpoints, ${n.burstRate}/min burst rate). High burst rates indicate rapid successive file edits — a retry-storm pattern that wastes round-trips and signals unclear prompts or brittle edits.`,action:`Review high-rework sessions: add clearer up-front specs or pre-edit hooks (lint/typecheck) to catch errors before they cascade into repeated checkpoint cycles.`,affected:r.length,evidence:r.map(e=>`${d(e.sessionId)}: score=${e.reworkScore}  churn=${e.churn}  burst=${e.burstRate}/min  span=${e.spanMin}m`),view:`files`}}},J=6,Y=1e3,Ar={id:`workflow.plan-missing-verification`,category:`workflow`,dataDeps:[],rule(e){let t=e.plans??[];if(t.length===0)return null;let n=t.filter(e=>!e.hasVerification&&(e.fileRefs>=J||e.words>=Y));if(n.length===0)return null;let r=t.length,i=Math.round(n.length/r*100),a=n.slice(0,5).map(e=>e.name);return{id:`workflow.plan-missing-verification`,category:`workflow`,severity:n.length>=3?`warning`:`info`,title:`Large plans are missing a Verification section`,detail:`${n.length} of ${r} plan(s) (${i}%) exceed the complexity threshold (${J}+ file refs or ${Y}+ words) and carry no ## Verification or ## Test section — the plans most likely to ship under-specified.`,action:`Add a ## Verification section to each large plan before running it. Describe the minimal observable signal that proves the plan succeeded.`,affected:n.length,evidence:a,fix:{target:`CLAUDE.md`,label:`Add plan Verification rule`,note:`Paste this into your CLAUDE.md to enforce a Verification section on large plans.`,snippet:`## Plan Verification discipline\n\nBefore executing any plan that touches ${J}+ files or exceeds ${Y} words, it MUST include a ## Verification section describing the minimal observable signal that proves the plan succeeded. No Verification section = plan is not ready to run.`,appliedMarkers:{headings:[/^##\s+plan verification/i],bodyPhrases:[`Verification section describing the minimal observable signal`,`plan is not ready to run`]}}}}},jr={compaction:`start fresh sessions or trim context sooner so the run does not have to compact`,"tool-errors":`add a PostToolUse validation hook so tool errors surface and get fixed before the next turn`,"tool-density":`plan or batch tool use instead of high-volume exploratory calls`,rhythm:`slow the rapid-fire turns — plan the next action before firing it`,"tool-repetition":`reuse earlier results instead of re-running the same call`},X=e=>`${Math.round(e*100)}%`;function Mr(e){return`${e.title}: ${X(e.high.goodRate)} good ${e.high.label} vs ${X(e.low.goodRate)} ${e.low.label} (${e.magnitude})`}var Nr={id:`workflow.harmful-habit`,category:`workflow`,dataDeps:[`timelines`,`tokenData`,`toolData`,`apiErrors`],rule(e){let t=Me(e.timelines??[],e.tokenData,e.toolData,e.apiErrors,new Map),n=t.factors.filter(e=>e.verdict===`hurts`);if(n.length===0)return null;let r=n[0],i=r.low.goodRate-r.high.goodRate>=.25?`warning`:`info`,a=jr[r.key];return{id:`workflow.harmful-habit`,category:`workflow`,severity:i,title:`${r.title} tracks with worse session outcomes`,detail:`Sessions ${r.high.label} have a ${X(r.high.goodRate)} good-outcome rate vs ${X(r.low.goodRate)} for ${r.low.label} ones (${r.magnitude}), across ${t.totalSessions} sessions. Outcome is proxied from cost and cleanliness for sessions you have not labelled good/bad.`,action:a?`Compare the two sides in the Patterns view, then ${a}.`:`Compare the two sides in the Patterns view and decide whether to change the habit.`,affected:r.high.sessionCount,view:`patterns`,evidence:[...n.slice(0,3).map(Mr),...r.high.examples.map(e=>d(e.sessionId))]}}},Pr={headings:[/^##\s+(?:Known paths|File locations|Project map|Corrections|Gotchas)\b/i],bodyPhrases:[`not the first place the agent looked`]},Fr=5;function Ir(e){return`\`${l(e.succeeded,80)}\` is the real path (the agent first tried \`${l(e.failed,80)}\`).`}var Lr={id:`workflow.correction-mining`,category:`workflow`,dataDeps:[`toolData`,`liveConfig`],rule(e){if(y(e.liveConfig,Pr))return null;let t=_e(pe(e.toolData));if(t.length===0)return null;let n=t.slice(0,Fr);return{id:`workflow.correction-mining`,category:`workflow`,severity:`info`,title:`Pin recurring wrong-path corrections in CLAUDE.md`,detail:`Mined ${t.length} wrong-path guess(es) from failed→fixed tool sequences — a file read/edit that errored, then succeeded at a different path with the same filename. Recording the real path stops future sessions re-guessing.`,action:`Add the corrected paths to your project CLAUDE.md so the agent uses the working location first.`,affected:t.length,evidence:n.map(e=>`${e.toolName}: ${l(e.failed,48)} → ${l(e.succeeded,48)}`+(e.occurrences>1?` (×${e.occurrences})`:``)),view:`tools`,fix:{target:`CLAUDE.md`,label:`Pin the corrections`,note:`Append to your project CLAUDE.md. These are facts the agent had to discover by failing first — pinning them avoids the failed attempt next time.`,snippet:`## Known paths & gotchas

These were learned the hard way — the agent guessed wrong, then found the right value. They are not the first place the agent looked, so pin them:

`+n.map(e=>`- ${Ir(e)}`).join(`
`),appliedMarkers:Pr}}}},Rr={id:`safety.dangerous-bypass`,category:`safety`,dataDeps:[`toolData`,`tokenData`,`permissionRows`,`liveConfig`],rule(e){let t=Ne(e.toolData);if(t.length===0||b(e.liveConfig?.settings,`deny`,ie))return null;let n=new Set(e.tokenData.filter(e=>o(e.entrypoint)).map(e=>e.sessionId)),r=e=>e.some(e=>n.has(e.sessionId)),i=Fe(t,e.permissionRows).filter(e=>e.bypassMode&&e.dangerousCount>0);if(i.length>0){let e=i.reduce((e,t)=>e+t.dangerousCount,0),n=new Set(i.map(e=>e.sessionId)),a=r(t.filter(e=>n.has(e.sessionId))),o=`critical`;return{id:`safety.dangerous-bypass`,category:`safety`,severity:a?g(o):o,unattended:a,title:`Dangerous commands ran under bypassed permissions`,detail:`${e} risky command(s) (e.g. rm -rf, git reset --hard, curl|sh) ran in ${i.length} session(s) that used bypassPermissions.`,action:`Reserve bypassPermissions for trusted, reversible work; add an explicit deny-list for destructive patterns.`,affected:e,evidence:t.slice(0,5).map(e=>`${d(e.sessionId)}, ${e.pattern}`),view:`permissions`,fix:{target:`settings.json`,label:`Add destructive-command deny rules`,note:`Merge into the "permissions" object in .claude/settings.json (deep-merge "deny"), and reserve bypassPermissions for trusted, reversible work. curl|sh cannot be prefix-matched, so curl/wget are denied wholesale — drop them if too broad.`,snippet:`{
  "permissions": {
    "deny": [
      "Bash(rm -rf:*)",
      "Bash(rm -fr:*)",
      "Bash(git reset --hard:*)",
      "Bash(git clean -fd:*)",
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(dd:*)",
      "Bash(mkfs:*)",
      "Bash(shred:*)",
      "Bash(curl:*)",
      "Bash(wget:*)"
    ]
  }
}`}}}if(b(e.liveConfig?.settings,`ask`,ne))return null;let a=r(t),s=`warning`;return{id:`safety.dangerous-commands`,category:`safety`,severity:a?g(s):s,unattended:a,title:`Dangerous command patterns detected`,detail:`${t.length} command(s) matched a destructive pattern (rm -rf, git push --force, dd, …).`,action:`Spot-check these were intentional; consider a hook that confirms before destructive ops.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${d(e.sessionId)}, ${e.pattern}`),view:`permissions`,fix:{target:`settings.json`,label:`Confirm before destructive commands`,note:`Merge into the "permissions" object in .claude/settings.json (deep-merge "ask"). "ask" forces an interactive confirmation rather than blocking outright.`,snippet:`{
  "permissions": {
    "ask": [
      "Bash(rm -rf:*)",
      "Bash(rm -fr:*)",
      "Bash(git reset --hard:*)",
      "Bash(git clean -fd:*)",
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(dd:*)",
      "Bash(mkfs:*)",
      "Bash(shred:*)"
    ]
  }
}`}}}},zr={id:`safety.prompt-friction`,category:`safety`,dataDeps:[`toolData`,`permissionRows`,`liveConfig`],rule(e){let t=Pe(e.toolData,e.permissionRows),n=t[0];if(!n||n.promptableCalls<20||n.share<25)return null;let r=n.toolName===`Bash`?oe:[n.toolName];return b(e.liveConfig?.settings,`allow`,r)?null:{id:`safety.prompt-friction`,category:`safety`,severity:`info`,title:`Reduce permission-prompt friction`,detail:`${n.toolName} accounts for ${n.share.toFixed(0)}% of prompt-eligible tool calls (${n.promptableCalls} across ${n.sessionCount} sessions).`,action:`Seed a project allowlist for the safe variants of this tool by merging the snippet below into .claude/settings.json. (If installed, the /fewer-permission-prompts skill can generate one from your transcripts.)`,affected:n.promptableCalls,evidence:t.slice(0,4).map(e=>`${e.toolName}: ${e.promptableCalls} (${e.share.toFixed(0)}%)`),view:`permissions`,fix:{target:`settings.json`,label:`Allowlist safe ${n.toolName} calls`,note:`Merge into .claude/settings.json — deep-merge the "permissions.allow" array. Allowed calls stop prompting; the Bash list below is limited to read-only/idempotent commands.`,snippet:n.toolName===`Bash`?JSON.stringify({permissions:{allow:oe}},null,2):JSON.stringify({permissions:{allow:[n.toolName]}},null,2)}}}},Br={id:`safety.deny-rule-never-triggered`,category:`safety`,dataDeps:[`toolData`,`liveConfig`],rule(e){let t=e.liveConfig?.settings?.permissions?.deny;if(!Array.isArray(t)||t.length===0)return null;let n=[];for(let r of t){if(typeof r!=`string`)continue;let{tool:t,specifier:i}=u(r);if(t===`Bash`&&i!==null){if(ce(r))continue}else if(i!==null)continue;let a=!1;for(let t of e.toolData){for(let e of t.calls)if(c(r,e)===!0){a=!0;break}if(a)break}a||n.push(r)}return n.length===0?null:{id:`safety.deny-rule-never-triggered`,category:`safety`,severity:`info`,title:`Unused permission deny rules`,detail:`${n.length} permission deny rule(s) never matched a command attempt in your retained history — they may be leftover clutter rather than active guards.`,action:`Review whether these are still needed; pruning dead entries keeps settings.json honest. Known dangerous-command guards are deliberately excluded from this check, so anything listed is a non-destructive rule that simply never fired.`,evidence:n,affected:n.length,view:`permissions`,fix:{target:`settings.json`,label:`Prune unused deny rules`,note:`Remove these never-matched entries from the "permissions.deny" array in settings.json if they are no longer relevant. (Destructive-command guards are never listed here.)`,snippet:JSON.stringify({permissions:{deny:n}},null,2)}}}},Vr={id:`safety.allow-rule-overlaps-deny`,category:`safety`,dataDeps:[`liveConfig`],rule(e){let t=e.liveConfig?.settings?.permissions,n=Array.isArray(t?.allow)?t.allow:[],r=Array.isArray(t?.deny)?t.deny:[];if(n.length===0||r.length===0)return null;let i=[];for(let e of n)if(typeof e==`string`){for(let t of r)if(typeof t==`string`&&ee(e,t)){i.push({allow:e,deny:t});break}}return i.length===0?null:{id:`safety.allow-rule-overlaps-deny`,category:`safety`,severity:`warning`,title:`Allow rules shadowed by a deny`,detail:`${i.length} permission allow rule(s) are fully covered by a deny rule — deny always wins, so the allow never takes effect.`,action:`Remove the dead allow entries (or narrow the deny if the allow was the intent) so settings.json reflects what actually happens.`,evidence:i.map(e=>`${e.allow}  ⊂  deny ${e.deny}`),affected:i.length,view:`permissions`,fix:{target:`settings.json`,label:`Remove shadowed allow rules`,note:`These "permissions.allow" entries are overridden by a broader "deny" (deny wins), so they do nothing. Delete them, or narrow the conflicting deny if the allow was intended.`,snippet:JSON.stringify({permissions:{allow:i.map(e=>e.allow)}},null,2)}}}},Hr=5,Ur={id:`security.model-deceit`,category:`security`,dataDeps:[`deceitSignals`],rule(e){let t=e.deceitSignals??[];if(t.length===0)return null;let n=t.filter(e=>e.assistantTurnCount>0&&e.unbackedClaimCount+e.contradictedClaimCount>0);if(n.length===0)return null;let r=0,i=0;for(let e of n)r+=e.unbackedClaimCount,i+=e.contradictedClaimCount;let a=n.flatMap(e=>e.claimSnippets.map(t=>`${d(e.sessionId)}: "${t}"`)).slice(0,Hr),o=i>0?`warning`:`info`,s=[];return i>0&&s.push(`${i} success claim(s) contradicted by a real verification failure`),r>0&&s.push(`${r} action claim(s) with no supporting tool evidence`),{id:`security.model-deceit`,category:`security`,severity:o,title:`Agent claimed work it cannot be shown to have done`,detail:`${s.join(` and `)} across ${n.length} session(s) — e.g. "all tests pass" with a failing run, or "I ran the suite" with no matching tool call.`,action:`Spot-check the flagged turns and require the agent to run and show verification before claiming completion (a Stop hook that blocks on unverified claims, or a CLAUDE.md "verify before done" rule).`,affected:i+r,view:`sessions`,evidence:a}}},Wr={id:`reliability.tool-errors`,category:`reliability`,dataDeps:[`toolData`,`liveConfig`],rule(e){if(_(e.liveConfig?.settings))return null;let t=Le(e.toolData).filter(e=>e.totalCalls>=5&&e.errorRate>=.2);return t.length===0?null:{id:`reliability.tool-errors`,category:`reliability`,severity:`warning`,title:`Tools with high error rates`,detail:`${t.length} tool(s) failed on ${(re*100).toFixed(0)}%+ of calls. Failed calls waste a round-trip and often trigger retries.`,action:`Investigate the top offenders — common causes are stale file state, wrong paths, or missing permissions.`,affected:t.reduce((e,t)=>e+t.errorCalls,0),evidence:t.slice(0,5).map(e=>`${e.toolName}: ${(e.errorRate*100).toFixed(0)}% of ${e.totalCalls}`),view:`errors`,fix:{target:`hook`,fixKind:`illustrative`,label:`Add a post-edit typecheck hook`,note:`Merge into the "hooks" object in .claude/settings.json. Swap the command for your project’s real typecheck/lint so broken edits surface immediately instead of compounding.`,snippet:`{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "npm run -s typecheck" }
        ]
      }
    ]
  }
}`}}}},Gr={headings:[/^##\s+Rate-limit hygiene\b/i],bodyPhrases:[`Avoid launching many parallel agent runs`]},Kr={id:`reliability.api-errors`,category:`reliability`,dataDeps:[`apiErrors`,`liveConfig`],rule(e){if(e.apiErrors.length===0)return null;let t=e.apiErrors.filter(e=>e.status!=null&&ae.has(String(e.status)));return t.length>0&&y(e.liveConfig,Gr)?null:t.length>0?{id:`reliability.rate-limits`,category:`reliability`,severity:`warning`,title:`Hitting rate-limit / overloaded errors`,detail:`${t.length} API error(s) returned 429/529 (rate limited or overloaded), which stall and retry turns.`,action:`Pace heavy automated runs, or move them to the priority service tier / off-peak hours.`,affected:t.length,view:`errors`,fix:{target:`CLAUDE.md`,label:`Add pacing guidance`,note:`Append to your project (or ~/.claude) CLAUDE.md so heavy runs self-throttle. 429/529 are server-side signals with no client retry/tier setting.`,snippet:`## Rate-limit hygiene\n\nHit ${t.length} rate-limit/overload (429/529) error(s) recently.\n- Avoid launching many parallel agent runs at once; serialize heavy automated batches.\n- Schedule large unattended runs (migrations, bulk refactors) for off-peak hours.\n- Break big jobs into smaller chunks with breaks between them rather than one long burst.`,appliedMarkers:Gr}}:{id:`reliability.api-errors`,category:`reliability`,severity:`info`,title:`API errors recorded`,detail:`${e.apiErrors.length} API error event(s) were logged across your sessions.`,action:`Open Errors to see the status-code breakdown and whether any pattern is recurring.`,affected:e.apiErrors.length,view:`errors`}}},qr={id:`reliability.retry-storms`,category:`reliability`,dataDeps:[`toolData`],rule(e){let t=Ie(e.toolData).filter(e=>e.hasErrors&&e.count>=4);return t.length===0?null:{id:`reliability.retry-storms`,category:`reliability`,severity:`info`,title:`Retry storms on failing tools`,detail:`${t.length} run(s) of 4+ back-to-back same-tool calls included errors — the same operation retried repeatedly.`,action:`Check whether a different approach (or fixing the root error once) avoids the retry loop.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${d(e.sessionId)}, ${e.toolName} ×${e.count}`),view:`errors`}}},Jr={id:`reliability.settings-json-invalid`,category:`reliability`,dataDeps:[`liveConfig`],rule(e){let t=e.liveConfig?.settingsHealth;if(!t||!t.present||t.ok)return null;let n=t.findings.filter(e=>e.severity===`error`);return n.length===0?null:{id:`reliability.settings-json-invalid`,category:`reliability`,severity:`warning`,title:`settings.json has validation errors`,detail:`Your settings.json has ${n.length} error-severity finding(s) (syntax, type mismatch, or unknown key); Claude Code silently ignores an invalid file, so your model/permissions/hooks may have no effect.`,action:`Open Config Hygiene to see the exact bad path and expected type, then fix settings.json.`,affected:n.length,evidence:n.slice(0,5).map(e=>`${e.path}: ${e.message}`)}}},Yr=3,Xr={id:`reliability.hook-errors`,category:`reliability`,dataDeps:[`runtimeEvents`,`liveConfig`],rule(e){let t=e.runtimeEvents;if(!t||t.length===0)return null;let{errorEvents:n}=x(t);if(n<Yr)return null;let r=e.liveConfig!=null,i=f(e.liveConfig?.settings),a=r&&!i;return{id:`reliability.hook-errors`,category:`reliability`,severity:a?`info`:`warning`,title:a?`Stop hooks errored in the past (none configured now)`:`Stop hooks are firing with errors`,detail:a?`Stop hooks errored on ${n} event(s) across your history. No Stop hook is configured in settings.json now, so this is historical — an erroring guard (lint, typecheck, git, notify) would have silently done nothing on every affected turn.`:`Stop hooks errored on ${n} event(s) across your history; an erroring guard (lint, typecheck, git, notify) silently does nothing on every affected turn.`,action:a?`No action needed unless you re-add a Stop hook; if you do, make sure its command handles errors so it does not fail silently. (visible in stop_hook_summary lines)`:`Inspect the failing hook command (visible in stop_hook_summary lines), fix it or add error handling so it does not fail silently.`,affected:n,provenance:{observations:[{claim:`${n} Stop-hook error event(s) recorded across history`,source:`parse-runtime-events`,field:`aggregateStopHooks().errorEvents`,value:n},{claim:i?`a Stop hook is currently configured`:r?`no Stop hook is currently configured`:`current settings.json could not be read`,source:`settings.json`,field:`hooks.Stop`}],inference:a?`The error count is historical and no Stop hook is configured now, so the failure is not current — wording is demoted to past tense.`:`A Stop hook is (or may be) configured, so the historical error count likely reflects an ongoing silent-failure risk.`},view:`errors`}}},Zr=5,Qr={id:`reliability.hook-prevented-continuation`,category:`reliability`,dataDeps:[`runtimeEvents`],rule(e){let t=e.runtimeEvents;if(!t||t.length===0)return null;let{preventedContinuations:n}=x(t);return n<Zr?null:{id:`reliability.hook-prevented-continuation`,category:`reliability`,severity:`warning`,title:`Stop hooks are blocking agent continuation`,detail:`${n} stop-hook event(s) returned a blocking exit, halting the session mid-run — most disruptive to unattended (sdk-*) workflows.`,action:`Review the blocking hook; if the block is unintentional, gate on the command exit code (or append "|| true") so it only blocks on real failures.`,affected:n,view:`errors`,fix:{target:`hook`,label:`Gate the hook on exit code`,note:`Restructure the Stop hook so it only blocks on a genuine failure. A hook blocks when its command exits non-zero; gate it (or append "|| true") so advisory output never halts the run.`,snippet:`{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "your-check.sh || true" }
        ]
      }
    ]
  }
}`}}}},$r=50,ei={id:`reliability.dropped-assignments`,category:`reliability`,dataDeps:[`teams`],rule(e){let t=e.teams??[];if(!t.length)return null;let n=t.filter(e=>e.droppedCount>0);if(n.length===0)return null;let r=n.filter(e=>e.stalledAgents.length>0),i=n.filter(e=>e.droppedPct>=$r),a=r.length>0||i.length>0?`warning`:`info`,o=n.reduce((e,t)=>e+t.droppedCount,0),s=n.reduce((e,t)=>e+t.totalAssignments,0),c=n.flatMap(e=>e.stalledAgents.map(t=>`${e.teamId}/${t.agent}`)).slice(0,5);return{id:`reliability.dropped-assignments`,category:`reliability`,severity:a,title:`Task assignments silently dropped by worker agents`,detail:c.length>0?`${o}/${s} task assignment(s) dropped across ${n.length} team(s); stalled agents (never started): ${c.join(`, `)}.`:`${o}/${s} task assignment(s) dropped across ${n.length} team(s) — workers never acknowledged the work.`,action:`Re-dispatch the unread tasks or split a stalled agent's queue across fresh workers.`,affected:o,evidence:n.flatMap(e=>e.droppedAssignments.map(t=>`${e.teamId}/${t.agent} [${t.taskId}] "${t.subject}" (unread ${t.ageMinutes}m)`)).slice(0,5),fix:{target:`command`,fixKind:`manual`,label:`Redispatch unread tasks`,note:`If you have the claude-team CLI, run this for each affected team to re-queue the unacknowledged assignments. Otherwise re-dispatch them from your team tooling by hand.`,snippet:n.slice(0,3).map(e=>`claude-team redispatch --team ${e.teamId} --only-unread`).join(`
`)}}}},ti={id:`reliability.self-update-health`,category:`reliability`,dataDeps:[`updateResults`],rule(e){let t=e.updateResults??[];if(!t.length)return null;let n=Re(t);if(n.failedCount===0)return null;let r=`${(n.successRate*100).toFixed(0)}%`,i=n.errorCodes.length>0?n.errorCodes.join(`, `):`unknown error`,a=n.cadenceDays==null?`n/a (single record)`:`~1 update / ${n.cadenceDays.toFixed(1)}d`,o=n.successRate<.7?`warning`:`info`;return{id:`reliability.self-update-health`,category:`reliability`,severity:o,title:`CLI self-update health`,detail:`Across ${n.total} captured update snapshot(s), ${n.successCount} succeeded (${r}). ${n.failedCount} failed/blocked`+(n.errorCodes.length?` (${i})`:``)+`. ${n.immediateRetries} immediate retry(s). Cadence: ${a}. Version drift: ${n.versionDrift??`unknown`}.`,action:o===`warning`?`Pin a known-good version or investigate the failing update path (${i}) before trusting auto-update.`:`Some update attempts failed (${i}). Monitor for recurrence; pin a version if failures become frequent.`,affected:n.failedCount,view:`recommendations`}}};function ni(e){let{attribution:t,sessions:n}=e;if(!t||t.length===0)return{};let r=new Set((n??[]).filter(e=>o(e.entrypoint)).map(e=>e.sessionId));if(r.size===0)return{};let i={};for(let e of t)if(r.has(e.sessionId))for(let[t,n]of Object.entries(e.mcpServers))i[t]=(i[t]??0)+n.invocations;return i}function ri(e){return[`# Add to the top of your CI/cron entrypoint, before launching claude:`,`MCP_AUTH_STATUS=$(claude mcp auth --check 2>/dev/null; echo $?)`,`if [ "$MCP_AUTH_STATUS" = "1" ]; then`,`  echo "::error::MCP re-auth required (${e.join(`, `)}) — run claude mcp auth interactively, then retry"`,`  exit 1   # fail fast instead of a silent zero-progress run`,`fi`].join(`
`)}function ii(e){return e.map(e=>`claude mcp auth ${e}`).join(`
`)}var ai={id:`reliability.mcp-needs-auth`,category:`reliability`,dataDeps:[`attribution`,`sessions`],rule(e){let t=e.mcpAuth;if(!t||!t.serversNeedingAuth.length)return null;let{blocking:n,advisory:r}=ze(t,ni(e));if(n.length===0&&r.length===0)return null;let i=n.map(e=>e.name),a=r.map(e=>e.name),o=n.length+r.length,s=n.length>0?`critical`:`warning`,c;c=n.length>0&&r.length>0?`${n.length} blocking MCP server(s) (${i.join(`, `)}) and ${r.length} advisory server(s) (${a.join(`, `)}) need interactive re-auth. Unattended (CI/cron/sdk) sessions cannot complete an OAuth flow — every tool call to a blocking server will fail silently.`:n.length>0?`${n.length} load-bearing MCP server(s) (${i.join(`, `)}) need interactive re-auth. Unattended sessions cannot complete an OAuth flow — every tool call to these servers will fail, burning tokens with no progress.`:`${r.length} MCP server(s) (${a.join(`, `)}) need re-auth. These are not currently called by unattended sessions, but interactive runs will fail until re-authenticated.`;let l=n.length>0?`Run the re-auth commands interactively BEFORE your next automated run, then add the pre-flight GATE to your CI/cron entrypoint to fail fast on future expired tokens.\n\nRe-auth:\n${ii(i)}`:`Run \`claude mcp auth <server>\` interactively for each flagged server: ${a.join(`, `)}.`,u=n.length>0?{target:`command`,label:`Copy pre-flight GATE`,note:`Add to the top of your CI/cron entrypoint before launching claude.`,snippet:ri(i)}:void 0;return{id:`reliability.mcp-needs-auth`,category:`reliability`,severity:s,title:n.length>0?`${n.length} blocking MCP server(s) need re-auth — unattended runs will fail`:`${o} MCP server(s) need re-auth`,detail:c,action:l,affected:o,unattended:n.length>0,evidence:[...i,...a].map(e=>`claude mcp auth ${e}`),fix:u}}},oi=10080*60*1e3,si=1,ci={id:`reliability.config-drift`,category:`reliability`,dataDeps:[`configBackups`],rule(e,t){let n=e.configBackups??[];if(!n.length)return null;let r=t-oi,i=n.filter(e=>e.kind!==`global-churn`&&e.timestamp>=r);if(i.length<si)return null;let a=i.filter(e=>e.severity===`warning`),o=i.filter(e=>e.kind===`server-disabled`),s=a.length>0?`warning`:`info`,c=[...new Set(o.map(e=>e.server).filter(Boolean))],l=c.length?` MCP server(s) disabled for a project: ${c.join(`, `)}.`:``,u=[...new Set(i.map(e=>e.project).filter(e=>!!e))],d=a.sort((e,t)=>t.timestamp-e.timestamp).slice(0,5).map(e=>`${new Date(e.timestamp).toISOString().slice(0,16)+`Z`}${e.project?` [${e.project}]`:``}${e.server?` server=${e.server}`:``} ${e.kind} ${e.from??`?`} -> ${e.to??`?`}`);return{id:`reliability.config-drift`,category:`reliability`,severity:s,title:o.length?`MCP server silently disabled — tool may have stopped working`:`Project MCP/config drifted in the last 7 days`,detail:`${i.length} config-drift event(s) detected in the last 7 days across ${u.length} project(s).`+l+` ${a.length} event(s) need attention (trust flip, server disabled, blanket-enable on).`,action:o.length?`Re-enable the disabled server(s) in the affected project's MCP settings, or re-accept the trust dialog if the tool should still be active.`:`Review the flagged config changes in ~/.claude/backups/ to confirm they were intentional.`,affected:i.length,evidence:d,...u.length?{projects:u}:{}}}},li={id:`reliability.agent-report-card`,category:`reliability`,dataDeps:[`sessionRegistry`,`telemetry`,`debugLogs`],rule(e){let t=de(e.sessionRegistry,e.telemetry,e.debugLogs);if(!t.projects.length)return null;let n=t.projects.filter(e=>e.verdict===`MOVE`),r=t.projects.filter(e=>e.verdict===`FLAG`);if(!n.length&&!r.length)return null;let i=n.filter(e=>e.attributionBucket===`low-signal`),a=n.filter(e=>e.attributionBucket!==`low-signal`),o=n.length?`warning`:`info`,s=[...n,...r],c=s.slice(0,6).map(e=>`${h(e.cwd)||e.cwd}: ${e.verdict} — ${e.dominantEntrypoint} ${e.dominantShare}% / drag ${e.dragScore} (${e.dragBucket})`);return{id:`reliability.agent-report-card`,category:`reliability`,severity:o,title:`Agent Report Card: some projects aren’t a clean KEEP`,detail:`Joined attribution (sessions) with reliability (telemetry + debug) across ${t.totalProjects} project(s): ${t.tally.KEEP} KEEP, ${t.tally.FLAG} FLAG, ${t.tally.MOVE} MOVE. Fleet retry-storm rate ${t.fleetRetryStormPct}%, ${Math.round(t.fleetWastedMs/1e3)}s dead wall-clock. `+(n.length?[a.length?`${a.length} project(s) carry heavy reliability drag despite committed attribution`:``,i.length?`${i.length} project(s) have too little evidence (<=2 sessions) to trust — pilot elsewhere`:``].filter(Boolean).join(`; `)+`.`:`${r.length} project(s) need a reliability fix or a CLI decision before trusting their cost.`),action:`Open the Agent Report Card. For MOVE projects the CLI is actively expensive here; for FLAG, fix the reliability tax (or pick one entrypoint) before trusting per-task cost.`,affected:s.length,view:`report-card`,evidence:c}}},Z=.05;function Q(e){return Date.parse(e)}function ui(e,n){let r=new Map,i=0;for(let a of e){let e=n.get(a.sessionId);if(!(!e||e.length===0))for(let n of a.entries){let o=Q(n.timestamp);if(Number.isNaN(o)||!e.some(e=>o>=e.start&&o<=e.end)||n.cacheReadTokens<=0)continue;let s=n.model||`unknown`;r.set(C(a.sessionId,s),(r.get(C(a.sessionId,s))??0)+n.cacheReadTokens);let c=t(s).cacheRead;i+=n.cacheReadTokens/1e6*c*Z}}let a=0;for(let e of r.values())a+=e;return{scopeKeys:[...r.keys()],cacheReadTokens:a,estSavingsUsd:i}}function di(e,t){let n=Q(e),r=Q(t);return Number.isNaN(n)||Number.isNaN(r)?null:{start:Math.min(n,r),end:Math.max(n,r)}}var fi=`reliability.retry-prefix-rewaste`,pi={id:fi,category:`reliability`,dataDeps:[`toolData`,`tokenData`],rule(e){let t=Ie(e.toolData).filter(e=>e.hasErrors);if(t.length===0)return null;let n=new Map;for(let e of t){let t=di(e.startTimestamp,e.endTimestamp);if(!t)continue;let r=n.get(e.sessionId)??[];r.push(t),n.set(e.sessionId,r)}if(n.size===0)return null;let{scopeKeys:r,cacheReadTokens:i,estSavingsUsd:a}=ui(e.tokenData,n);if(r.length===0||i<=0)return null;let o={leverId:fi,category:`reliability`,cause:`failed-tool-retry`,orderKey:12,ownedPools:[`cacheRead`],scopeKeys:r,counterfactual:{kind:`scaleTokens`,poolDeltaFrac:{cacheRead:Z}},evidenceTokens:i},s=new Set(r.map(e=>e.split(`|`)[0]));return{id:fi,category:`reliability`,severity:`info`,title:`Failed-tool retries re-pay the cached prefix`,detail:`${t.length} errored retry run(s) replayed their turn across ${s.size} session(s); each replay re-reads the cached prefix. A conservative ${Math.round(Z*100)}% of the in-window cache-read (~${(i/1e6).toFixed(2)}M tokens) is re-paid waste — only the re-paid residual, not the legitimate co-located work.`,action:`Fix the root tool error once instead of re-running it — a single failed Bash/Edit/MCP call replays the whole turn and re-feeds its cache-read prefix. Check the Errors view for the recurring failure.`,estSavingsUsd:a,reclaim:o,affected:s.size,evidence:t.slice(0,5).map(e=>`${d(e.sessionId)}, ${e.toolName} ×${e.count}`),view:`errors`}}},$=`reliability.overload-reretry`,mi=9e4,hi={id:$,category:`reliability`,dataDeps:[`apiErrors`,`tokenData`],rule(e){let t=e.apiErrors.filter(e=>e.status!=null&&ae.has(String(e.status))&&typeof e.retryAttempt==`number`&&e.retryAttempt>1);if(t.length===0)return null;let n=new Map;for(let e of t){let t=Date.parse(e.timestamp);if(Number.isNaN(t))continue;let r=n.get(e.sessionId)??[];r.push({start:t-mi,end:t+mi}),n.set(e.sessionId,r)}if(n.size===0)return null;let{scopeKeys:r,cacheReadTokens:i,estSavingsUsd:a}=ui(e.tokenData,n);if(r.length===0||i<=0)return null;let o={leverId:$,category:`reliability`,cause:`failed-tool-retry`,orderKey:14,ownedPools:[`cacheRead`],scopeKeys:r,counterfactual:{kind:`scaleTokens`,poolDeltaFrac:{cacheRead:Z}},evidenceTokens:i},s=new Set(r.map(e=>e.split(`|`)[0])),c=t.reduce((e,t)=>Math.max(e,t.retryAttempt??0),0);return{id:$,category:`reliability`,severity:`info`,title:`Overload re-retries re-pay the cached prefix`,detail:`${t.length} overload/rate-limit (429/529) error(s) re-attempted (retryAttempt up to ${c}) across ${s.size} session(s); each re-attempt re-reads the cached prefix. A conservative ${Math.round(Z*100)}% of the in-window cache-read (~${(i/1e6).toFixed(2)}M tokens) is re-paid waste.`,action:`Pace heavy automated runs and schedule large unattended batches off-peak so 429/529 backoff re-attempts stop re-feeding the cached prefix.`,estSavingsUsd:a,reclaim:o,affected:s.size,evidence:t.slice(0,5).map(e=>`${d(e.sessionId)}, ${e.status} attempt ${e.retryAttempt}`),view:`errors`}}},gi=2e3,_i=5e3,vi=5;function yi(e){return`${(e/1e3).toFixed(1)}s`}var bi=[et,nt,ft,pt,At,Mt,Pt,an,sn,ln,un,dn,Rr,zr,Br,Vr,Wr,Kr,qr,{id:`activity.stale-projects`,category:`activity`,dataDeps:[`projects`,`liveConfig`],rule(e,t){if(typeof e.liveConfig?.settings?.cleanupPeriodDays==`number`)return null;let n=t-672*60*60*1e3,r=e.projects.filter(e=>e.sessionCount>=3&&e.lastSeen>0&&e.lastSeen<n);return r.length===0?null:(r.sort((e,t)=>e.lastSeen-t.lastSeen),{id:`activity.stale-projects`,category:`activity`,severity:`info`,title:`Projects have gone quiet`,detail:`${r.length} project(s) with 3+ sessions have had no activity in over 4 weeks.`,action:`If these are done, archive them; if not, they may be stalled and worth a check-in.`,affected:r.length,evidence:r.slice(0,5).map(e=>`${e.projectShort}, last ${te(e.lastSeen,t)}d ago`),view:`activity`,fix:{target:`settings.json`,label:`Auto-clean old transcripts`,note:`Add to ~/.claude/settings.json to age out idle history automatically. WARNING: this permanently deletes local transcripts older than the given days — including the data this dashboard reads. Default is 30; raise the number to keep more.`,snippet:`{
  "cleanupPeriodDays": 28
}`}})}},Lt,_t,xt,Ct,Vt,Tt,Jr,Ot,Xr,Qr,Wt,qt,Sn,wn,En,jn,Pn,Un,Yn,Zn,Qn,dr,_r,xr,Er,ei,{id:`activity.activity-trend`,category:`activity`,dataDeps:[`statsCache`],rule(e,t){let n=e.statsCache;if(!n)return null;let r=Be(n,t);if(r.verdict!==`hotter`||r.toolCallCount.pctChange<50)return null;let{pctChange:i,thisWeek:a,lastWeek:o}=r.toolCallCount,s=n.lastComputedDate.slice(0,10),c=r.stale,l=c?`Tool-call rate ran ${i}% hotter than the prior week (as of ${s})`:`Tool-call rate is running ${i}% hotter than last week`,u=c?`As of ${s} (latest computed week — stats-cache.json may be stale; run the CLI to refresh): ${a.toLocaleString()} tool calls vs ${o.toLocaleString()} the prior week (+${i}%). At that pace the rolling 5-hour or weekly cap was being approached sooner than expected.`:`This week so far: ${a.toLocaleString()} tool calls vs ${o.toLocaleString()} last week (+${i}%). At this pace you may hit the rolling 5-hour or weekly cap sooner than expected.`;return{id:`activity.activity-trend`,category:`activity`,severity:i>=200?`warning`:`info`,title:l,detail:u,action:c?`Refresh the CLI stats (run any Claude command) to get a current read, then consider spreading heavy work across more sessions.`:`Consider cooling off — spread heavy work across more sessions, or check your 5h/weekly usage before starting the next large task.`,affected:a,evidence:[`tool calls this week: ${a.toLocaleString()}`,`tool calls last week: ${o.toLocaleString()}`,`sessions this week: ${r.sessionCount.thisWeek}`,`sessions last week: ${r.sessionCount.lastWeek}`,`source: stats-cache.json (lastComputedDate: ${n.lastComputedDate})`],provenance:{observations:[{claim:`${a.toLocaleString()} tool calls in the current week window`,source:`stats-cache.json`,field:`lastComputedDate / per-day toolCallCount (this week)`,value:a},{claim:`${o.toLocaleString()} tool calls in the prior week window`,source:`stats-cache.json`,field:`per-day toolCallCount (last week)`,value:o}],inference:`Week-over-week tool-call rate is +${i}% (>= 50% threshold), so at this pace the rolling 5h / weekly cap may be reached sooner than expected.`,asOf:s,stale:c},view:`activity`}}},kr,Ar,ti,ai,ci,li,pi,hi,Nr,Lr,Ur,{id:`speed.hook-overhead`,category:`speed`,dataDeps:[`runtimeEvents`],rule(e){let t=e.runtimeEvents;if(!t||t.length===0)return null;let{timedEvents:n,meanTimedDurationMs:r,maxDurationMs:i}=x(t);return n<vi||r<gi?null:{id:`speed.hook-overhead`,category:`speed`,severity:r>=_i?`warning`:`info`,title:`Stop hooks are adding seconds to every turn`,detail:`Stop hooks add about ${yi(r)} of wall-clock per turn (mean over ${n} timed stop event(s), up to ${yi(i)}). That latency is paid on every turn the agent finishes, slowing the whole session.`,action:`Make the slow Stop hook asynchronous (fire-and-forget) so it stops blocking each turn, or drop it if its output is only advisory. Per-hook timing is sparse, so identify the offender from the hook commands in your settings.json.`,affected:n,view:`agents`,fix:{target:`hook`,label:`Run the slow Stop hook async`,note:`Background the slow Stop hook so its wall-clock no longer lands on every turn. A synchronous hook blocks until its command returns; detaching it (fire-and-forget) keeps the side effect without the per-turn latency. Only do this for advisory hooks — a hook that must gate the turn has to stay synchronous.`,snippet:`{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "nohup your-slow-hook.sh >/dev/null 2>&1 &" }
        ]
      }
    ]
  }
}`}}}},nn];function xi(e){let t={...e,liveConfig:e.liveConfig??null,assistantFeatures:e.assistantFeatures??null};if(`modelPinSavings`in e)return t;let n=rt({tokenData:e.tokenData});return n?{...t,modelPinSavings:n}:t}var Si=new WeakMap;function Ci(e,t){let n=t===void 0;if(n){let t=Si.get(e);if(t)return t}let r=t??Date.now(),i=[];for(let t of bi){let n=t.rule(e,r);n&&i.push(n)}let a=i.sort((e,t)=>{let n=m[t.severity]-m[e.severity];if(n!==0)return n;let r=(t.estSavingsUsd??0)-(e.estSavingsUsd??0);return r===0?(t.affected??0)-(e.affected??0):r});return n&&Si.set(e,a),a}function wi(e){let t=e.savingsAttribution?.signatureId;if(t)return`sig:${t}`;let n=e.savingsAttribution?.interventionKey;return n?`int:${n}`:`id:${e.id}`}function Ti(e){return e.category===`cost`&&e.id!==`cost.expensive-sessions`}function Ei(e){let t=new Map;for(let n of e){if(!Ti(n))continue;let e=n.estSavingsUsd??0;if(e<=0)continue;let r=wi(n),i=t.get(r);(!i||e>i.usd)&&t.set(r,{usd:e,category:n.category})}let n=0,r={};for(let{usd:e,category:i}of t.values())n+=e,r[i]=(r[i]??0)+e;return{total:n,byCategory:r}}function Di(e){return Ei(e).total}export{bn as a,Qe as c,L as i,Ye as l,Ci as n,I as o,Di as r,vn as s,xi as t};